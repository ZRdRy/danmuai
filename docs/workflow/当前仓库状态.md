# 当前仓库状态

> 每个工单完成后更新。模板见 [templates/当前仓库状态/当前仓库状态模板.md](../templates/当前仓库状态/当前仓库状态模板.md)。

**最后更新**：2026-06-10（W-DOCS-REORG-001 Markdown 文档分布整理）

---

## 最近变更（W-DOCS-REORG-001 Markdown 文档分布整理）

- **归档**：153 份完成报告 → `docs/archive/completion-reports/`；26 份历史工单 → `docs/archive/workorders/`
- **模板瘦身**：`docs/templates/Codex完成报告/` 仅留 `Codex完成报告模板.md`；`docs/templates/工单/` 仅留 `工单模板.md`
- **子目录**：新建 `docs/core/`、`docs/features/`、`docs/agent/`、`docs/operations/`；协作活文档迁入 `docs/workflow/`
- **顶层**：`docs/*.md` 剩 6 个（含硬编码 `DANMAKU_FORMULA.md`）；根目录 `.md` 剩 5 个（`THIRD_PARTY_NOTICES.md`、`README.en.md` 已移入 docs）
- **链接**：`AGENTS.md`、`docs/README.md`、`docs/workflow/README.md` 已更新；`docs/reports/` 职责改为审查/审计类报告
- **未改动**：`app/`、`main.py`、`tests/`、`scripts/`、`docs/architecture-governance/`
- **验证**：`python scripts/boundary_guard.py` PASS；`test_boundary_guard_*.py` 分批 PASS
- **完成报告**：[W-DOCS-REORG-001-完成报告](../archive/completion-reports/W-DOCS-REORG-001-完成报告.md)
- **工单**：[W-DOCS-REORG-001](../工单列表/工单/W-DOCS-REORG-001.md)

---

## 最近变更（W-OPEN-CONSOLE-FALLBACK-001 托盘 pywebview 失败后弹窗询问浏览器回退）

- **范围**：`app/main_launch_mixin.py::_open_web_console` 删除原 `shell.handshake_failed → return` 静默分支；新增 `_prompt_browser_fallback_after_webview_failure` helper：主线程弹 QMessageBox，Yes → `open_web_console_browser(server, path)` + 设 `_browser_launch_opened=True`；No → 静默；已 fallback 过 → 跳过弹窗（保留 BUG-014 dedupe）
- **影响入口**：托盘右键「设置」菜单 / 双击托盘图标 / `show_settings()` / `DanmuApp.start()` 缺 API key 时的 `_open_web_console("/#settings")`
- **统一行为**：`web_launch_mode == "webview"` 与 `"browser"` 走相同 fallback 路径（用户确认 q2）
- **i18n**：新增 `webview.fallback_to_browser_title` / `webview.fallback_to_browser_message` 键（沿用 `tr(key, default)` 第二参数为默认文案，与 `webview.slow_start_*` 模式一致，未在 `translations_*.py` 表注册）
- **未改动**：`main.py`、`app/main_lifecycle_mixin.py` 等其他 main mixin、`app/web_console.py`、`app/webview_shell.py` 主体、`app/tray.py`、`app/translations.py`、`web/`、`tests/test_web_launch.py`、`tests/test_webview_shell.py`、`requirements*.txt`、锁文件、`.github/`、`DanmuAI.spec`
- **既有契约改写**：`tests/test_capture_flow.py::test_open_web_console_after_handshake_failed_does_not_reopen_browser` 改写为 `test_open_web_console_after_handshake_failed_prompts_browser_fallback`（原 BUG-014 dedupe 由 `finalize_handshake_failure` + `_browser_launch_opened` 守护，不在本工单影响范围）
- **新增测试**：`test_open_web_console_after_handshake_failed_no_prompt_when_browser_already_opened`（dedupe）+ `test_open_web_console_after_handshake_failed_user_declines`（用户选 No）
- **验证**：分批 pytest 3 批通过（14 + 41 + 13 = 68 passed）；`python scripts/boundary_guard.py` PASS
- **完成报告**：[W-OPEN-CONSOLE-FALLBACK-001-completion-report](../archive/completion-reports/W-OPEN-CONSOLE-FALLBACK-001-completion-report.md)
- **工单**：[W-OPEN-CONSOLE-FALLBACK-001](../工单列表/工单/W-OPEN-CONSOLE-FALLBACK-001.md)

---

## 最近变更（W-PERSONA-DEFAULT-001 测试1–3 内置提示词 + 默认活跃 6 人格）

- **内置人格**：测试1–3 的 `system_zh` 更新为负责人本机调试的随机口吻/贴图风格提示（非固定五人剧本）
- **默认活跃**：`DEFAULT_ACTIVE` = 测试1、测试2、测试3、吐槽型、傲娇型、腹黑型；`active_personae_version` 升至 **7**（升级覆盖活跃列表）
- **置顶**：工坊列表仍置顶测试1–4，测试4 不在默认活跃中
- **验证**：`test_reply_contract.py` 31 passed；`test_web_persona_api.py` 25 passed
- **完成报告**：[W-PERSONA-DEFAULT-001-completion-report](../archive/completion-reports/W-PERSONA-DEFAULT-001-completion-report.md)

---

## 最近变更（W-PERSONA-TRIM-001 人格工坊删 4 人格 + 契约风格迁移）

- **人格**：移除内置路人惊讶型、捧场活跃型、搞笑玩梗型、轻度吐槽型；`DEFAULT_ACTIVE` 现为置顶测试1–4；`active_personae_version` 升至 **6**
- **输出契约**：仅 JSON/条数/字数/scene_brief；「像多位真实观众…」「禁 AI腔…」迁至系统提示补充默认前缀（`DEFAULT_SYSTEM_STYLE_*`）
- **Web**：人格工坊 `system_custom` 展示含默认风格；助手设置契约预览与后端一致
- **验证**：分批 pytest 69 passed（reply_contract、web_persona_api、reply_parser、web_auth）
- **完成报告**：[W-PERSONA-TRIM-001-completion-report](../archive/completion-reports/W-PERSONA-TRIM-001-completion-report.md)

---

## 最近变更（W-OVERVIEW-GLOBAL-FIELDS-001 温馨控制台顶部迁移提示内容/昵称）

- **UI**：「提示内容」（`live_topic`）与「昵称」（`user_nickname`）从人格工坊迁至温馨控制台顶部（公告条下、状态栏上）
- **前端**：`loadOverviewGlobalFields()` 在 `init()` 与切回 overview 时加载；保存逻辑不变（`PUT /api/config`）
- **字段提示**：`OVERVIEW_FIELD_TIPS` 挂载于 `#page-overview`
- **验证**：`test_bundle_paths.py` 21 passed；`test_web_server.py` 28 passed
- **完成报告**：[W-OVERVIEW-GLOBAL-FIELDS-001-completion-report](../archive/completion-reports/W-OVERVIEW-GLOBAL-FIELDS-001-completion-report.md)

---

## 最近变更（W-SCENE-MEMORY-DEFAULTS-OFF-001 场景记忆默认关闭）

- **范围**：仅修改 `CONFIG_DEFAULTS` 中 `scene_memory_enabled` / `prompt_dedup_enabled` 默认值从 `"1"` 改为 `"0"`；`scene_memory_interval_sec=5` 保留（与 `normal_recognition_interval_sec=5` 对齐）
- **生效路径**：`ConfigStore.__init__` 在首装 / 缺 `danmu_speed` 时调用 `seed_config_defaults` 把 `CONFIG_DEFAULTS` 写入 SQLite；**新装用户三键值均为 `"0"`**
- **不影响**：
  - 已存用户 `config.db` 中旧值 `"1"` **保留**（本工单不主动改写）
  - Web UI 三个 checkbox 仍展示（`fillForm` 反映 `cfg.scene_memory_enabled === '1'`，新装用户=0 即未勾选）
  - `app/main_request_context_mixin.py::_scene_memory_enabled` / `_prompt_dedup_enabled` 的 `default="1"` 形参**未改**（W-SCENE-MEMORY-REMOVE-001 §需求 8 范围）
  - `app/scene_memory.py` / `app/memory/` 兼容模块 / `parse_ai_reply_with_memory` 全部**保留**（W-SCENE-MEMORY-REMOVE-001 §需求范围）
- **未改动**：`main.py`、`app/main_*_mixin.py`、`app/web_api/*`、`app/overlay.py`、`app/danmu_engine.py`、`app/reply_parser.py`、`app/persona_contract.py`、`web/static/**`、`tests/`、`scripts/`、`requirements*.txt`、`.github/`、`DanmuAI.spec`、锁文件
- **现有测试 0 改动均 PASS**：默认值断言不存在；FakeConfig / stub 显式覆盖；`test_config_service_normalize` 的 `_normalize_items` clamp 测试与默认值无关
- **后续工单**：W-SCENE-MEMORY-REMOVE-001-EXECUTE（按规划执行删除）、W-SCENE-MEMORY-REMOVE-002 / 003（后续剥离）
- **完成报告**：[W-SCENE-MEMORY-DEFAULTS-OFF-001-completion-report](../archive/completion-reports/W-SCENE-MEMORY-DEFAULTS-OFF-001-completion-report.md)
- **工单**：[W-SCENE-MEMORY-DEFAULTS-OFF-001](../工单列表/工单/W-SCENE-MEMORY-DEFAULTS-OFF-001.md)

---

## 最近变更（W-SCENE-MEMORY-REMOVE-001 删除场景记忆 — 规划阶段）

- **范围**：本次为**调研 + 规划阶段**，**未改任何业务代码**；待负责人按工单正文 §需求 列表执行实施
- **摸排结果**：「场景记忆」功能相关 **后端 16 个文件 / Web 5 个文件 / 测试 11 个文件 / 文档 7 个文件**已 100% 定位
- **关键边界澄清**：
  - 删：「助手设置」UI 三个控件 + `WEB_CONFIG_KEYS` / `CONFIG_DEFAULTS` 三键 + `ConfigService._normalize_items` 四段 + `_scene_memory_enabled` / `_prompt_dedup_enabled` 默认 `"1"` → `"0"` + `start()` 不再 `reset()`
  - 保留：`scene_generation` 字段（运行期恒 0 兼容字段；与 request meta 复合 key / `DanmuItem` / `drop_pending_below_generation` 绑定，ISSUE-014 待 W-020）
  - 保留：`QueuedReply.memory_eligible`（与 `source in ("ai", "mic")` 派生语义）
  - 保留：`app/scene_memory.py` 兼容 re-export / `app/memory/` 五模块 / `parse_ai_reply_with_memory` / `build_normal_reply_contract_zh/en`（业务侧未决定是否同步调整 AI 模型契约，登记到 ISSUE-051）
- **兼容性**：`GET /api/config` 仍返回三个键（GET 兼容）；`PUT /api/config` 写入静默忽略；旧 `config.db` 三个键值**保留**
- **测试影响**：`test_reply_parser.py` 13 用例 / `test_reply_contract.py` 6 用例 / `test_memory_prompt_builder.py` 4 用例 / `test_scene_brief.py` 10 用例 / `test_danmu_dedup.py` / `test_p0_main_flow.py` / `test_request_scheduling.py` / `test_floating_panel_consume.py` / `test_mic_insert.py` 既有 stub 与用例**不动**（路径保留）；`test_config_service_normalize.py` 4 个用例需改写；`test_config_round_trip.py` 删 3 行；`test_web_auth.py` / `test_bundle_paths.py` / `test_config_changed_combo.py` 改写
- **未执行**：构建 / 测试 / boundary_guard（按 IDE_AGENT_RULES §10 留待实施工单）
- **完成报告（规划阶段）**：[W-SCENE-MEMORY-REMOVE-001-completion-report](../archive/completion-reports/W-SCENE-MEMORY-REMOVE-001-completion-report.md)
- **工单**：[W-SCENE-MEMORY-REMOVE-001](../工单列表/工单/W-SCENE-MEMORY-REMOVE-001.md)
- **建议后续工单**：W-SCENE-MEMORY-REMOVE-001-EXECUTE（立即执行本工单 §需求）、W-SCENE-MEMORY-REMOVE-002（完全剥离 ISSUE-051-a/b/c/d/h）、W-SCENE-MEMORY-REMOVE-003（运行态字段清理 ISSUE-051-e/f/g）

---

## 最近变更（W-WEBSEARCH-REMOVE-001 删除联网搜索）

- **联网搜索**：完整移除后端门控模块、Provider 能力声明、AI 请求注入、配置键 `web_search_enabled`、模型目录 `supports_web_search`、Web `web_search_likely_supported` 导出
- **前端**：助手设置「API 与模型」tab 移除「联网增强」checkbox 与动态提示；`settings-providers.js` 同步清理
- **测试**：删除 `tests/test_web_search.py`；相关用例从 `test_ai_client` / `test_web_auth` / `test_model_catalog` / `test_provider_adapters` 移除
- **遗留**：旧 `config.db` 中 `web_search_enabled` 不主动清理，运行时不再读取
- **验证**：分批 pytest 144 passed（含排除 ISSUE-050 的 1 项）；`boundary_guard.py` PASS
- **完成报告**：[W-WEBSEARCH-REMOVE-001-completion-report](../archive/completion-reports/W-WEBSEARCH-REMOVE-001-completion-report.md)

---

## 最近变更（W-REMOVE-DOUBAO-TTS-001 读弹幕移除豆包 TTS）

- **读弹幕 TTS**：移除「火山豆包语音」平台；仅保留 MiMo（默认）与百炼 Qwen3
- **后端**：删除 `DoubaoTtsAdapter`、`DOUBAO_VOICES`、`tts_app_id`；存量 `tts_provider=doubao` GET 仍可读，保存/试听/朗读显式报错引导改选
- **Web**：读弹幕设置页移除豆包 option 与 App ID 输入；`index.html` 已 rebuild
- **探测脚本**：ISSUE-049 已修复 — `scripts/probe_tts_providers.py` 改为 MiMo + 百炼（移除豆包/VOLCENGINE）
- **验证**：分批 pytest 44 passed；`boundary_guard.py` PASS
- **完成报告**：[W-REMOVE-DOUBAO-TTS-001-completion-report](../archive/completion-reports/W-REMOVE-DOUBAO-TTS-001-completion-report.md)

---

## 最近变更（W-DEFAULTS-002 首装默认配置）

- **人格**：移除内置「专业分析型」；`DEFAULT_ACTIVE` 现为 4 人格；`active_personae_version` 升至 4 自动剔除旧活跃项
- **烂梗公式化**：`meme_barrage_collect_batch_size` / `meme_barrage_display_batch_size` 默认 **2**（间隔仍为 5 秒）
- **场景记忆**：`scene_memory_enabled` 默认 **1**（首装 / 恢复默认）
- **桌宠**：`pet_command_box_enabled` 已为 1，本次无改动
- **验证**：分批 pytest 57 passed（config_defaults、persona_api、meme_barrage_api、config_service_normalize、export_web_config_defaults）
- **完成报告**：[W-DEFAULTS-002-completion-report](../archive/completion-reports/W-DEFAULTS-002-completion-report.md)

---

## 最近变更（烂梗采集游标重启续读）

- **游标持久化**：`meme_barrage_local_read_offset` / `meme_barrage_remote_page_num` 写入 `config.db`；`MemeBarrageService` 启动加载，采集与 `reset_cursors` 时写回
- **行为**：进程完全退出再启动从上次采集位置续读；切换分类/标签或「清除」仍重置游标
- **未改动**：`main_meme_mixin`、Web API、展示队列内存 FIFO
- **验证**：`pytest tests/test_meme_barrage_runtime.py tests/test_meme_barrage_api.py -q -x` → 23 passed

---

## 最近变更（场景记忆更新间隔）

- **配置**：新增 `scene_memory_interval_sec`（默认等于识屏间隔；服务端 snap 为 `normal_recognition_interval_sec` 整数倍，最大 12 倍）
- **运行时**：`_scene_memory_update_due` 门控 `set_brief`；非刷新轮次仍注入已保存 brief
- **Web**：API 设置区新增「记忆更新间隔（秒）」；`step`/`min` 随识屏间隔联动
- **验证**：`pytest tests/test_scene_brief.py tests/test_config_service_normalize.py tests/test_bundle_paths.py -q`；`boundary_guard.py` PASS

---

## 最近变更（读弹幕移除自定义 OpenAI 兼容 TTS）

- **读弹幕 TTS**：移除「自定义（OpenAI 兼容）」平台；仅保留 MiMo / 豆包 / 百炼；PUT/probe 提交 `custom_openai` 返回 400
- **后端**：删除 `OpenAiCompatAudioTtsAdapter` 与 `tts_catalog` 中 `custom_openai` 条目；遗留落库配置 GET 仍可读，定时朗读跳过并提示改选预设平台
- **Web**：`settings.html` / `app.js` 移除自定义 API 地址与模型名输入；`index.html` 已 rebuild
- **未改动**：助手设置 `/api/custom-models`（视觉/聊天自定义模型）保持不变
- **验证**：`pytest tests/test_danmu_read_api.py tests/test_danmu_tts.py tests/test_tts_catalog.py -q` → 35 passed

---

## 最近变更（Scene Brief 记忆重构）

- **记忆模型**：四档 `memory_mode` 替换为 `scene_memory_enabled`（场景简述）与 `prompt_dedup_enabled`（prompt 层防重复）两个独立开关
- **AI 契约**：`persona_contract` 与 `reply_parser` 统一为 `{"scene_brief":"…","comments":[…]}`；内存仅保留最近一条 `scene_brief`
- **删除**：`app/memory/scene_context.py`、`app/memory/visual_update.py`；`SceneMemoryStore` → `SceneBriefStore`
- **Web**：助手设置改为「场景记忆」「避免重复弹幕」两个 checkbox；移除 `memory_mode` / `memory_window` UI
- **迁移**：启动时 `memory_mode` 自动映射到新键；`python web/static/build_index_html.py` 已重建 `index.html`
- **验证**：`pytest tests/test_scene_brief.py tests/test_reply_parser.py tests/test_reply_contract.py tests/test_config_service_normalize.py tests/test_mic_insert.py -q`；`boundary_guard.py` PASS

---

## 最近变更（W-IDE-TEST-BATCH-001）

- **IDE Agent 测试规范**：`docs/IDE_AGENT_RULES.md` 新增 §10 — 禁止本地全量 pytest；四批策略、源文件→测试映射、分批测试报告模板、`.pytest_tmp` 清理
- **文档同步**：`AGENTS.md` §7 / 附录 A、`TEST_COMMANDS.md`、Codex 完成报告模板、当前仓库状态模板 — 区分 IDE Agent 本地 vs CI/维护者全量
- **验证**：纯文档工单；`git diff` 确认未改 `app/`、`tests/`、`main.py`
- **完成报告**：[W-IDE-TEST-BATCH-001-completion-report](../archive/completion-reports/W-IDE-TEST-BATCH-001-completion-report.md)

---

## 最近变更（W-DARK-TOOLTIP-001）

- **tooltip 主题**：新增 `--tooltip-bg` / `--tooltip-fg`；`.warm-tooltip`、`.ui-tooltip-float` 不再复用 `--color-text-primary` 作背景，修复黑夜模式浅底白字不可读
- **验证**：`pytest tests/test_console_theme.py -q` → 6 passed
- **完成报告**：[W-DARK-TOOLTIP-001-completion-report](../archive/completion-reports/W-DARK-TOOLTIP-001-completion-report.md)

---

## 最近变更（W-API-MODE-VISION-001）

- **API 模式**：`#api_mode` 由 `initApiModeSelect()` 填充「豆包 / OpenAI 兼容」；官方服务商预设下锁定模式；`fillForm` 在渲染视觉模型前先回填 `api_mode`
- **视觉模型**：`loadModelCatalog` 检查 HTTP 状态，失败时 toast 提示；冷启动后选择器与已保存模型回填顺序加固
- **验证**：`pytest tests/test_web_server.py::test_web_api_mode_select_initialized -q` → passed
- **完成报告**：[W-API-MODE-VISION-001-completion-report](../archive/completion-reports/W-API-MODE-VISION-001-completion-report.md)

---

## 最近变更（PET-017 桌宠动画展示修复）

- **默认 idle**：`resolve_base_animation` 恒返回 `idle`；未拖拽时不再因 `ai_in_flight` / 屏上弹幕切到 review/run 行（取代 PET-007 常驻 Agent 联动；one-shot 与拖动交互不变）
- **spritesheetLayout**：`pet.json` 可选 `petdex`（默认）或 `yuexin-miao`；内置 `data/pet/default/pet.json` 标注后者；`PetAssetPack.frame_rect` 按布局解析 running-left/right 行号，修复自定义 PetDex 标准包拖动朝向反转
- **验证**：`pytest tests/test_pet_assets.py tests/test_pet_animation_mapper.py tests/test_pet_window_drag.py tests/test_pet_command_service.py -q` → 43 passed
- **完成报告**：[pet-017-completion-report](../archive/completion-reports/pet-017-completion-report.md)

---

## 最近变更（启动性能中/低优修复）

- **W-STARTUP-MEDLOW-PERF-001（S-003/S-021/S-005）**：桌宠 spritesheet 懒加载；emoji/CJK 快路径；托盘双击提示 + i18n 修正
- **W-STARTUP-MEDLOW-WEB-001（S-006/S-009）**：`maybe_restart_web_console`（最多 3 次）；`_capture_fail_streak` 连续截图告警
- **W-STARTUP-MEDLOW-QUEUE-001（S-017/S-014）**：`drop_replaceable_fallbacks` 接线；prepend 优先丢 fallback；`ai_worker_pool()`
- **验证收口**：S-025 `memory_window` 已有测试；S-026 补 `test_ws_logs_max_connections_capped`
- **验证**：定向 pytest 130 passed；`boundary_guard.py` → PASS
- **完成报告**：[W-STARTUP-MEDLOW](../archive/completion-reports/W-STARTUP-MEDLOW-completion-report.md)

---

## 最近变更（启动性能极高/高优修复）

- **W-INFLIGHT-RECOVER-001（S-011/S-024）**：`VISUAL_INFLIGHT_RECOVER_SEC=48`；`_try_recover_stale_visual_inflight` 清理 meta/timing 并释放 in-flight
- **W-AI-REQUEST-DEADLINE-001（S-012）**：`REQUEST_WALL_CLOCK_SEC=45`；流式读循环 + `AiRunnable` 墙上时钟
- **W-STARTUP-NONBLOCK-001（S-001/S-002）**：`_ensure_server_ready` 短探测；`quit()` join Web 线程 3s
- **W-STARTUP-UX-OBS-001（S-004/S-016）**：托盘 `show_webview_starting_hint`；`reply_queue_trim` warning
- **验证**：定向 pytest 58 passed；`boundary_guard.py` → PASS
- **完成报告**：[W-STARTUP-RUNTIME-HIGH](../archive/completion-reports/W-STARTUP-RUNTIME-HIGH-completion-report.md)

---

## 最近变更（W-MEDLOW-001～005）

- **主链路（W-MEDLOW-001）**：eager `RequestScheduler`/`RequestTimingService`；`purge_stale` 清理孤儿 RTT；QPixmap `copy`；移除 `_meme_ai_in_flight`；空 persona prompt 守卫
- **Web API（W-MEDLOW-002）**：`_invoke_main` 400/500 映射；`DIAGNOSTICS_SSE_INTERVAL_SEC`；WEB_CONSOLE 鉴权策略三节表
- **Web 前端（W-MEDLOW-003）**：读弹幕缓存失效 + tab 切换刷新；`isMaskedApiKey`；移除 settings 死 option
- **AI/配置（W-MEDLOW-004）**：`AiWorker._stopping` → `threading.Event`；base64 key 自动升级 Fernet；`set_batch` executemany
- **显示/测试（W-MEDLOW-005）**：`DEFAULT_DANMU_FONT_FAMILY`；`test_mic_orchestrator` / `test_meme_barrage_client`
- **验证**：`pytest tests/ -q` → 1091 passed；`boundary_guard.py` → PASS
- **完成报告**：[W-MEDLOW-BATCH](../archive/completion-reports/W-MEDLOW-BATCH-completion-report.md)

---

## 最近变更（W-DANMU-POOL-FORMULA-CACHE-001 + W-MEME-RUNTIME-OPT-001）

- **公式化文本缓存（DP-001/002）**：`is_stored_custom_pool_text` / `is_stored_meme_barrage_text` 懒加载 set；`ConfigStore` 写 custom pool / meme 库后 `invalidate_formula_text_cache`；新增 `meme_barrage_library_all_texts`
- **pool topup（DP-007）**：`maybe_pool_topup` 对 `entry_zone_overloaded` 使用 `callable` 守卫
- **烂梗 local（DP-003）**：采集 tick 跳过 `ingest_collected_texts`，直接入展示队列
- **展示防重入（DP-004）**：`_meme_display_tick` 经 `__dict__["_meme_display_ticking"]` 防重入
- **DP-005**：审查标注为已关闭（悬浮窗 `elidedText`，不入库截断）
- **验证**：`pytest tests/test_danmu_pool.py tests/test_meme_barrage_runtime.py tests/test_meme_barrage_api.py -q` → 30 passed
- **完成报告**：[W-DANMU-POOL-FORMULA-CACHE-001](../archive/completion-reports/W-DANMU-POOL-FORMULA-CACHE-001-completion-report.md)、[W-MEME-RUNTIME-OPT-001](../archive/completion-reports/W-MEME-RUNTIME-OPT-001-completion-report.md)

---

## 最近变更（W-RENDER-TOPMOST-BATCH-001 弹幕渲染与置顶审查 R-001～R-012）

- **悬浮窗 consume（R-001）**：`main._consume_reply_queue` 在 `floating_panel` 模式 pop 前预检空文本/去重；意外上屏失败回插队首
- **Win32 穿透/置顶（R-002/R-003）**：`showEvent` 重设 `apply_overlay_exstyles`；置顶 health tick 1500ms→500ms
- **去重对齐（R-004）**：`danmu_engine_dedup.is_duplicate_in_recent` 横向/悬浮窗共用 Levenshtein
- **速度钳位（R-005）**：`danmu_speed` 经 `config_service` 钳位 0.5–10.0
- **体验项（R-006～R-012）**：公式化弹幕卡片省略号、字号热更新竖向重排、`screen_index_fallback_warning` 琥珀提示、独占全屏误报降敏（`WS_CAPTION`）、`max_items` 不瞬删在屏条、顶部渐隐
- **验证**：`pytest tests/ -q` → 1074 passed；`python scripts/boundary_guard.py` → PASS
- **完成报告**：[W-RENDER-TOPMOST-BATCH-001-完成报告](../archive/completion-reports/W-RENDER-TOPMOST-BATCH-001-completion-report.md)

---

## 最近变更（W-CONFIG-UI-LINK-001 配置与 UI 联动审查修复）

- **死配置清理**：移除 `floating_panel_click_through`、`floating_panel_lifetime_sec`（`CONFIG_DEFAULTS` / `WEB_CONFIG_KEYS` / `_normalize_items`）
- **写入层归一化**：`danmu_speed`（0.1–10.0）、`dedup_threshold`（0.0–1.0）、`empty_accel` 布尔、`pet_position_x/y`（-32000–32000）
- **验证**：`pytest tests/test_config_defaults.py tests/test_web_console.py tests/test_web_routes.py tests/test_floating_panel_engine.py -q` → 74 passed
- **工单**：[W-CONFIG-UI-LINK-001](../工单列表/工单/W-CONFIG-UI-LINK-001.md)、[完成报告](../templates/Codex完成报告/W-CONFIG-UI-LINK-001-完成报告.md)

---

## 最近变更（极高 + 高优先级问题修复批次）

- **W-QUIT-TEARDOWN-001**：`quit()` 先 `web_server.stop()` 再 `config.close()`；`ConfigStore._conn_usable()` 防御；`test_get_meta_after_config_close`
- **W-WIN32-LAYERED-001**：`win32_overlay_zorder.apply_overlay_exstyles` / `stack_hwnd_above`；overlay / 悬浮窗 / 桌宠去重 Win32 常量
- **W-CS-ENCRYPT-DRY-001**：`_encrypted_get` / `_encrypted_set`；base64 分支直写 SQL（避免 ISSUE-042 嵌套锁）
- **W-MAIN-INFLIGHT-ATOMIC-001**：`_acquire_visual_inflight` + `test_acquire_and_release_visual_inflight_atomic`
- **W-TEST-AI-ERROR-001**：生产阈值 5 次退避、402/403 fatal；`_on_ai_error` 增加 `402` 判定
- **W-TEST-REPLY-PARSER-001** / **W-TEST-CONFIG-NORMALIZE-001** / **W-TEST-MAIN-E2E-001**：新增/扩充测试
- **验证**：相关 pytest 120 passed；`boundary_guard.py` PASS
- **完成报告**：[W-HIGH-PRIORITY-BATCH-completion-report](../archive/completion-reports/W-HIGH-PRIORITY-BATCH-completion-report.md)

---

## 最近变更（W-OVERLAY-TOPMOST-001 游戏场景 Overlay 置顶重申）

- **运行期置顶**：`DanmuApp` 在 `engine.running` 期间每 1.5s 调用 `reassert_topmost_zorder()`（`scrolling` / `floating_panel` 互斥层）；`start()` / `stop()` 启停 `_topmost_health_timer`；焦点变化即时重申
- **Win32 共用**：`app/win32_overlay_zorder.py`（`reassert_hwnd_topmost` + `probe_exclusive_fullscreen_risk`）
- **兼容性提示**：`/api/status.overlay_compat_warning` + 运行概览琥珀条；`danmu_render_mode` 设置说明补充无边框/窗口化全屏建议
- **验证**：`pytest tests/test_overlay_topmost_health.py tests/test_floating_panel_overlay.py tests/test_overlay_render.py tests/test_web_status.py -q` → 56 passed；`python scripts/boundary_guard.py` → PASS
- **工单**：[W-OVERLAY-TOPMOST-001](../工单列表/工单/W-OVERLAY-TOPMOST-001.md)

---

## 最近变更（W-MEME-COLLECT-SETTINGS-001 烂梗采集配置热更新）

- **问题**：Web「烂梗公式化 → 采集配置」保存成功但 interval/batch 未影响运行时采集
- **根因**：`apply_meme_barrage_settings()` 复用 `_start_meme_barrage_timers()`，每次保存 `singleShot(0)` 立即采集；活跃 timer 未强制按新 interval 重启
- **修复**：新增 `_sync_meme_barrage_timer_intervals()`；热更新只重排 interval，引擎 `start()` 保留首轮 immediate tick
- **验证**：`pytest tests/test_meme_barrage_runtime.py tests/test_meme_barrage_api.py tests/test_meme_barrage_multi_tag.py -q` → 33 passed
- **完成报告**：[W-MEME-COLLECT-SETTINGS-001-completion-report](../archive/completion-reports/W-MEME-COLLECT-SETTINGS-001-completion-report.md)

---

## 最近变更（PET-016 桌宠右键菜单「退出应用」）

- **桌宠右键菜单**：「关闭桌宠」后增加分隔线与「退出应用」，调用 `DanmuApp.quit()`（与托盘退出同路径）
- **实现**：`app/pet/pet_window.py` 抽出 `_build_context_menu()`；新增 `tests/test_pet_context_menu.py`
- **验证**：`pytest tests/test_pet_context_menu.py tests/test_pet_window_drag.py` → 11 passed
- **完成报告**：[pet-016-completion-report](../archive/completion-reports/pet-016-completion-report.md)

---

## 最近变更（W-DEFAULTS-001 首装默认配置与桌宠倍率）

- **视觉 API 首装/恢复默认**：`api_mode=openai`、服务商预设对应「自定义 OpenAI 兼容接口」（空 endpoint/model）、`temperature=0.8`
- **桌宠**：`pet_scale` 默认 `0.5`（CONFIG_DEFAULTS、pet_state、Web 桌宠页）
- **麦克风**：`mic_api_endpoint` 恢复默认仍为火山方舟地址（与视觉 API 拆分）
- **前端**：`guessProviderIdFromEndpoint` 与 Python registry 对齐（空 endpoint + openai → `custom_openai`）
- **验证**：`pytest tests/test_web_auth.py tests/test_config_store.py tests/test_model_providers.py` 等 164 项通过
- **完成报告**：[W-DEFAULTS-001-完成报告](../templates/Codex完成报告/W-DEFAULTS-001-完成报告.md)

---

## 最近变更（W-CONTENT-HINTS-001 三页字段提示补齐）

- **范围**：人格工坊、公式化弹幕库、桌宠的可配置项补齐 ℹ️ 悬浮字段说明，与助手设置体验一致
- **实现**：`settings-hints.js` 新增 `PERSONA_FIELD_TIPS` / `DANMU_POOL_FIELD_TIPS` / `PET_FIELD_TIPS` 与 `initContentPageFieldHints()`；`app.js` 启动时调用
- **HTML**：`content-pages.html` 补 `for` / 区块 title `id`；`index.html` 已 rebuild
- **验证**：`python -m pytest tests/test_web_server.py::test_web_content_page_field_hints_wired -q` → 1 passed
- **完成报告**：[W-CONTENT-HINTS-001-完成报告](../templates/Codex完成报告/W-CONTENT-HINTS-001-完成报告.md)

---

## 最近变更（助手设置 Tab 重命名与顺序调整）

- **Tab 更名**：「节奏与图像」→「识图清晰度」；「读弹幕」→「AI读弹幕」
- **Tab 顺序**：API 与模型 → 麦克风模式 → AI读弹幕 → 弹幕显示 → 识图区域 → 识图清晰度 → 字体设置
- **文件**：`web/static/partials/settings.html`、`web/static/partials/sidebar.html`、`web/static/index.html`（rebuild）
- **验证**：`python -m pytest tests/test_bundle_paths.py -q` → 19 passed

---

## 最近变更（W-FP-V3-003 floating_panel 竖向间距）

- **引擎**：`FloatingPanelEngine` 增加 `can_accept_new_item` / `estimate_entry_delay_ms`；`min_gap=max(12, height*0.25)`；`add_text` 底部空间不足时拒绝进入
- **主链路**：`main._consume_reply_queue` floating_panel peek-before-pop（spacing 阻塞不 pop）；`_estimated_reply_gap_ms` 独立分支，不复用横向 `visibility_counts`
- **测试**：`tests/test_floating_panel_engine.py` 间距用例；新增 `tests/test_floating_panel_consume.py`
- **验证**：floating panel 相关 24 passed；`test_danmu_dedup` + `test_p0_main_flow` 29 passed；`boundary_guard` PASS

---

## 最近变更（读弹幕多平台 TTS）

> **历史记录**（2026-06 初版）。豆包 TTS 已于 W-REMOVE-DOUBAO-TTS-001 移除；探测脚本 ISSUE-049 已对齐 MiMo+百炼，见上文 W-REMOVE-DOUBAO 条目。

- **TTS 适配层**：`app/tts_providers.py` 曾新增火山豆包 V3 HTTP、百炼 Qwen3 HTTP/Realtime WebSocket 适配器；`app/tts_catalog.py` 提供平台/模型/音色目录
- **探测脚本**：`scripts/probe_tts_providers.py`（密钥仅环境变量）；百炼 `qwen3-tts-flash-2025-11-27` 与 realtime 模型探测通过
- **Web**：读弹幕设置页曾支持 MiMo / 豆包 / 百炼 / 自定义；`GET /api/danmu-read/catalog`；`index.html` 已 rebuild
- **依赖**：`requirements.txt` 增加 `dashscope>=1.24.6`
- **测试**：`tests/test_tts_catalog.py`、`tests/test_danmu_tts.py`、`tests/test_danmu_read_api.py` 扩展

---

## 最近变更（开放从下到上弹幕显示模式）

- **Web 设置**：解除 `danmu_render_mode=floating_panel`（从下到上）的「开发中」拦截；选中时显示悬浮窗子项、隐藏横向弹幕专属项（`#scrollingModeFields`）
- **配置**：`floating_panel_lifetime_sec` 表单项改为 `floating_panel_speed`（0.5–5.0）；`floating_panel_speed` 加入 `WEB_CONFIG_KEYS` 与归一化
- **文件**：`web/static/modules/settings-core.js`、`settings-defaults.js`、`settings-hints.js`、`partials/settings.html`、`index.html`（rebuild）、`app/application/config_service.py`、`tests/test_config_defaults.py`、`tests/test_web_routes.py`

---

## 最近变更（移除前台窗口活动记忆）

- **删除**：`app/window_info.py`、`app/memory/activity.py`、`app/memory/activity_prompt.py` 及对应测试（`test_activity*`、`test_window_info.py`）
- **主链路**：`_capture_screenshot` 不再调用 `_collect_activity_observation`；`_append_scene_memory_to_user_pt` 始终走场景记忆卡片（不再注入「近期状态：…」）
- **运行态**：移除 `_activity_state`、`_last_activity_collect_at`；`GenerationPipelineState` 不再暴露 `last_activity_collect_at`
- **文档**：`docs/main-pipeline-sequence.md`、`docs/runtime-state-map.md` 已同步
- **测试**：`python -m pytest tests/test_scene_memory.py tests/test_p0_main_flow.py tests/test_ai_pipeline.py tests/test_diagnostics_snapshot.py -q`；`python scripts/boundary_guard.py`

---

## 最近变更（PET-015）

- **Web 自定义桌宠开放**：`web/static/modules/app-pet-page.js` 不再把 `asset_source/path` 强制写回内置默认；页面现在会保留并提交当前素材来源与路径，修复“保存其他桌宠设置后自定义素材丢失”的问题。
- **原生目录导入**：`app/pet/pet_facade.py` 新增 `import_pet_asset_via_dialog()` 与 `reset_pet_asset_to_builtin()`；`app/web_api/pet.py` / `app/web_api/routes.py` 新增 `POST /api/pet/import-folder`、`POST /api/pet/reset-asset`，通过主线程目录选择器校验并切换本地桌宠。
- **桌宠页体验补齐**：`web/static/partials/content-pages.html` 与 `web/static/index.html` 新增 PetDex 中文站入口、当前素材来源/路径展示、错误提示和“恢复默认桌宠”按钮；导入说明文案已改为真实格式要求。
- **测试**：
  - `python -m pytest tests/test_pet_assets.py tests/test_web_routes.py tests/test_bundle_paths.py -q` → **64 passed**
  - `python -m pytest tests/test_pet_window_drag.py tests/test_pet_window_transparency.py -q` → **14 passed**
  - `python -m pytest tests/ -q -k pet` → **57 passed, 1 skipped, 1039 deselected**
  - `python scripts/boundary_guard.py` → **PASS**
- **完成报告**：[docs/reports/pet-015-completion-report.md](../archive/completion-reports/pet-015-completion-report.md)

---

## 最近变更（W-DOCTOR-B-FIX-001）

- **config_store**：4 处 `logger.error(...format(error=e))` 改为仅记录 `type(e).__name__`（含 `batch_write_failed`）
- **Web**：烂梗 meta 轮询失败 `console.warn`；模型 catalog 首次加载失败 `console.warn`（保留空 `{platforms:[]}` fallback）
- **Edge**：`community-register-guard` JSON 解析失败仍 400，增加 `console.error`（仅错误类型名）
- **backend-doctor 复测**：native **234 → 231**；B 类 `agent/swallowed-error` 3 条归零；`security/sensitive-logging` 3 条仍报（`api_key_write_failed` 翻译键误报）
- **测试**：`test_config_store` + `test_web_server` + `test_meme_barrage_api` → **48 passed**
- **完成报告**：[templates/Codex完成报告/W-DOCTOR-B-FIX-001-完成报告.md](../templates/Codex完成报告/W-DOCTOR-B-FIX-001-完成报告.md)

---

## 最近变更（W-PROBE-TIMEOUT-001）

- **fetch 超时**：[scripts/community/probe_vercel_bundle.mjs](../scripts/community/probe_vercel_bundle.mjs) 新增 `fetchWithTimeout()`（`FETCH_TIMEOUT_MS = 5000`）；`fetch(BASE)` 与 `fetch(url)` 均已替换
- **兼容性**：优先 `AbortSignal.timeout()`；旧 Node 回退 `AbortController` + `setTimeout`
- **backend-doctor 复测**（2026-06-08）：native finding **236 → 234**；`probe_vercel_bundle.mjs` 两处 `node/no-timeout-server-or-client` **归零**
- **A 类（人工复核口径）**：**2 → 0**（W-DOCTOR-REVIEW-001 所列 A 类已全部处理）
- **脚本试跑**：默认 Vercel URL 在本环境 5s 内超时退出（证明 timeout 生效）
- **完成报告**：[templates/Codex完成报告/W-PROBE-TIMEOUT-001-完成报告.md](../templates/Codex完成报告/W-PROBE-TIMEOUT-001-完成报告.md)

---

## 最近变更（W-CI-DOCTOR-001）

- **CI pin SHA**：[`.github/workflows/ci.yml`](../.github/workflows/ci.yml) 两行 `uses:` 改为完整 commit SHA：
  - `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5`（原 `v4`）
  - `actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065`（原 `v5`）
- **backend-doctor 复测**（2026-06-08）：native finding **240 → 236**；`ci/github-action-unpinned` 与 `supply-chain/unpinned-github-action` **归零**；`ci/missing-backend-doctor-gate` **仍 1**（未接 `--ci` gate）
- **A 类（人工复核口径）**：**6 → 2**（剩余 `probe_vercel_bundle.mjs` timeout ×2）
- **未改动**：`app/`、`web/`、`main.py`、`tests/`、`scripts/`、`requirements*.txt`、`.backend-doctor.toml`
- **完成报告**：[templates/Codex完成报告/W-CI-DOCTOR-001-完成报告.md](../templates/Codex完成报告/W-CI-DOCTOR-001-完成报告.md)

---

## 最近变更（W-FP-V3-001 / W-FP-V3-002）

- **基线冻结**：新增 [reports/floating-panel-v3-baseline-report.md](../archive/completion-reports/floating-panel-v3-baseline-report.md)，明确 DanmuAI V2 `floating_panel` 现状是“底入堆叠到目标位 + hold + fade out”，且 `floating_panel_speed` 未参与 V2 位移计算；参考项目仅取 `bililive_dm-master/Bililive_dm/MainWindow.xaml.cs` 的 `SideBar` 路径，不参考 `Full` 全屏横向模式。
- **引擎语义修复**：`app/floating_panel_engine.py` 移除 `target_y` / `fade_state` / lifetime 删除语义，改为条目从底部基线进入后持续上滚，`item.current_y + item.height < 0` 即删除。
- **速度接线**：`floating_panel_speed` 现在实际参与位移计算，固定映射为 `pixels_per_second = 120 * floating_panel_speed`；默认 `1.5` 对应 `180 px/s`。
- **渲染层保持不变**：`app/floating_panel_overlay.py` 保留右侧窄窗、圆角卡片、点击穿透、字体逻辑，仅配合连续上滚模型工作。
- **测试**：`python -m pytest tests/test_floating_panel_engine.py tests/test_floating_panel_overlay.py tests/test_status_floating_panel.py -q` → **18 passed**；覆盖持续上滚、速度映射、越顶删除、空队列停表与 `/api/status` 面板计数。
- **文档**：`docs/ARCHITECTURE.md` 的 `floating_panel` 描述已改为“底部进入、持续上滚、越顶移除”；新增 `W-FP-V3-001.md`、`W-FP-V3-002.md` 及对应完成报告。
- **未改动**：`main.py`、`app/overlay.py`、`app/danmu_engine.py`、`app/web_api/*`、`web/static/*`、`scripts/*`、依赖/锁文件/CI/打包配置。

---

## 最近变更（W-DOCTOR-REVIEW-001）

- **复核输入**：`reports/backend-doctor.json`（W-DOCTOR-IGNORE-001 复测，240 finding / 62 文件）
- **输出**：[backend-doctor-review-report.md](backend-doctor-review-report.md) — 全仓 **A6 / B6 / C102 / D126 / E0**
- **A 类（确认真实，6 条）**：`.github/workflows/ci.yml` Actions 未 pin SHA ×4；`scripts/community/probe_vercel_bundle.mjs` 外部 fetch 无 timeout ×2
- **B 类（边界，6 条）**：`config_store.py` sensitive-logging ×3；meme 轮询/catalog/Edge Function 吞错 ×3
- **重点结论**：`node/floating-promise` 54 条 Web UI → **C**；localhost fetch 无 timeout → **C**；tailwindcdn/community-site/venv 残留 → **D**
- **未改动**：`app/`、`web/`、`main.py`、`tests/`、`scripts/`、`.github/` 业务代码
- **完成报告**：[templates/Codex完成报告/W-DOCTOR-REVIEW-001-完成报告.md](../templates/Codex完成报告/W-DOCTOR-REVIEW-001-完成报告.md)

---

## 最近变更（W-DOCTOR-IGNORE-001）

- **配置落盘**：项目根新增 [`.backend-doctor.toml`](../.backend-doctor.toml)（TOML；`disabled-rules = ["security/hardcoded-secret", "node/dead-export"]`）
- **schema 修正**：`backend-doctor v0.1.0` 实际格式为 TOML（`init --yes` 生成），**非** 报告 §11.1 原规划的 `backend-doctor.config.json` + `ignore.files`/`ignore.rules`
- **复测结果**（2026-06-08）：
  - 总 finding：**15,663 → 240**（62 文件；扫描模式因配置存在而变化）
  - `security/hardcoded-secret`：**0** ✓
  - `node/dead-export`：**0** ✓
  - venv 桶：**37**（仍报；工具不支持文件级 ignore）
  - `community-site/`：**14**（仍报）
  - `tailwindcdn.js`：**19**（仍报）
  - 项目自身桶：**170**
  - `--score`：**0**（label=Critical；但 `caps` 已清空，无 `raw secret` cap）
  - 扫描耗时：**32.2s**（首扫 149.8s）
- **文档**：报告 §11 替换为 TOML 实际内容；新增 §11.4 工具限制、§11.5 复测对照表
- **未改动**：`app/`、`web/`、`main.py`、`tests/`、`scripts/`、`requirements*.txt`、`.github/`、`AGENTS.md`、`.gitignore`、所有 git 跟踪业务源文件
- **未执行**：`--fix-safe` / `--fix-guided` / `--ci` / `--min-score` / `pytest` / `ruff` / `boundary_guard`
- **完成报告**：[templates/Codex完成报告/W-DOCTOR-IGNORE-001-完成报告.md](../templates/Codex完成报告/W-DOCTOR-IGNORE-001-完成报告.md)
- **后续**：`W-CI-DOCTOR-001` 须评审 240 finding 基线；`W-DOCTOR-DEPENDENCY-001` 负责 `community-site/` 独立扫描

---

## 最近变更（W-RACE-001）

- **陈旧 `AiRunnable` 丢弃**：`main.py::_on_ai_reply` 入口新增代际校验 —— `_pop_request_meta` 返回空 meta（已被 `stop()` 清空）时打 `stale_reply_dropped` warning 并直接 `return`，不释放新会话 in-flight 槽位、不入队、不污染 token 统计。`_pop_request_meta` 既有 `request_meta_missing: reason=pop_before_reply` warning 保留作可观测性，本判断作为第二道防线。
- **新增测试**：`tests/test_p0_main_flow.py`（2 个用例）—— Case A 验证陈旧 reply 不上屏、不消耗 in-flight；Case B 验证正常路径不受影响。
- **既有测试行为变更后预注册 meta**：`tests/test_ai_pipeline.py` 3 例、`tests/test_danmu_engine.py` 1 例、`tests/test_live_freshness.py` 1 例 —— 既有行为对空 meta fallback 到默认 `source="visual"`，W-RACE-001 后空 meta 直接 return，必须预注册 meta 才走正常路径。
- **文档**：`docs/MAIN_PIPELINE.md` §4 加 W-RACE-001 bullet、§8 `reason=` 表新增 `stale_reply_dropped`；`docs/已知问题与后续事项.md` 登记 ISSUE-042。
- **测试**：定向 `test_p0_main_flow` 2 passed；`_on_ai_reply` 相关 8 文件 70 passed；全量（排除 4 个已知挂起文件）1059 passed, 4 skipped；`scripts/boundary_guard.py` PASS。
- **未改动**：`app/main_lifecycle_mixin.py`（start/stop 顺序）、`app/main_request_context_mixin.py`（`_pop_request_meta` 既有 warning）、`app/ai_client.py`、`app/runnable.py`、`app/web_api/*`、`app/overlay.py`、`app/danmu_engine.py`、`web/static/*`、`docs/main-pipeline-sequence.md`。
- **完成报告**：[templates/Codex完成报告/W-RACE-001-完成报告.md](../templates/Codex完成报告/W-RACE-001-完成报告.md)

---

## 最近变更（W-SEC-001）

- **/api/session 鉴权修复**：bug-audit/bug-03.md 缺陷 1 修复。新增 `app/web_console_session_auth.py::enforce_session_authorization`；`read_console_session` 强制 `Authorization: Bearer <token>` 或同源 loopback（Origin/Referer 与 Host 同为 127.0.0.1/localhost）握手；curl/外部进程/非 loopback 一律 401/403。
- **测试**：`tests/test_web_server.py` 新增 6 个 session 鉴权用例；定向 25 passed；全量 N passed, Y skipped。
- **未改动**：`main.py`、`app/main_*mixin.py`、`app/web_api/*`、`web/static/*`、打包/CI/锁文件。
- **完成报告**：[templates/Codex完成报告/W-SEC-001-完成报告.md](../templates/Codex完成报告/W-SEC-001-完成报告.md)

---

## 最近变更（烂梗标签多选 W-MEME-TAG-MULTI-001）

- **Web UI**：`web/static/modules/app-meme-barrage-page.js` 把 `selectedTag` 改为 `selectedTags: Set`，点击 toggle，强制至少保留 1 个；保存时发 `tag: Array.from(selectedTags)`。
- **Pydantic**：`MemeBarrageSettingsPayload.tag` 从 `str | None` 改为 `list[str] | None`（`app/web_api/routes.py`）。
- **存储**：`meme_barrage_tag` 由单字符串改为 JSON 数组；`save_settings` 兼容旧字符串/逗号字符串输入，落盘 `json.dumps(list, ensure_ascii=False)`（`app/web_api/meme_barrage.py`）。
- **读取**：`app/meme_barrage/config.py` 新增 `_parse_tag`，优先 `json.loads`，失败回退逗号分隔；空集兜底 `["06"]`，旧值无需显式迁移。
- **运行时**：`app/main_meme_mixin.py` 采集 tick 把 `settings["tag"]`（list）join 为逗号字符串再传 `MemeFetchRunnable`，远程 `/machine/sortAllBarrage?tags=` 接受逗号分隔。
- **测试**：新增 `tests/test_meme_barrage_multi_tag.py`（13 用例），更新 `tests/test_meme_barrage_api.py` 旧单字符串断言为 JSON 解码；定向 75 passed（含两个 batch），boundary_guard PASS。
- **未改动**：`main.py` 主链路、`app/meme_barrage/runnable.py`、`app/meme_barrage/client.py`、`app/overlay.py`、`docs/archive/`。

---

---

## 最近变更（W-ANTISLOP-SCAN-001-REPORT-FIX-002）

- **修正**：[backend-doctor-scan-report.md](backend-doctor-scan-report.md) **§11.1** 推荐 `backend-doctor.config.json` 的 `ignore.files` 增补 `"community-site/**"`，与 §11.2 预期 "`community-site/` 桶 32 条 → 0" 严格对齐
- **修正原因**：REPORT-FIX-001 版 §11.1 漏写 `community-site/**`，但 §11.2 仍写"32 → 0"，导致 config 与预期自相矛盾；用户二选一 → **方案 A**（保留 §11.2 预期 + 在 §11.1 增补 `community-site/**`）
- **与 §7.2「`community-site/` 应有独立扫描」不冲突**：在 `ignore.files` 中加 `community-site/**` **仅**在 DanmuAI **主仓**扫描中静默这 32 条；`community-site/` 自己的 `backend-doctor.config.json` 由 **`W-DOCTOR-DEPENDENCY-001`** 立项时按需建立（独立 `package.json` 根、独立规则集）
- **同步更新**：
  - [docs/工单列表/工单/W-DOCTOR-IGNORE-001.md](../工单列表/工单/W-DOCTOR-IGNORE-001.md) — §需求 1 的 JSON `ignore.files` 增补 `"community-site/**"`；§风险点由"不要把 `community-site/**` 写进 `ignore.files`"改为"`community-site/**` 已纳入 `ignore.files`（方案 A），与 §7.2 独立扫描不冲突"；§验收标准 `ignore.files` 列表增补 `"community-site/**"`
  - [docs/工单列表.md](工单列表.md) — W-ANTISLOP-SCAN-001 行允许修改文件列表追加 `W-ANTISLOP-SCAN-001-REPORT-FIX-002-完成报告.md`，备注中显式标注"`ignore.files` 已含 `community-site/**`，方案 A"
- **结论**：`community-site/**` **已**纳入 `W-DOCTOR-IGNORE-001` 的 `ignore` 范围（项目根 `backend-doctor.config.json`）；`community-site/` 自身**不**新建 `backend-doctor.config.json`（留给 `W-DOCTOR-DEPENDENCY-001`）
- **未改动**：`app/`、`web/`、`main.py`、`tests/`、`scripts/`、`requirements*.txt`、`.github/`、`AGENTS.md`、所有 git 跟踪的业务源文件
- **未落盘 `backend-doctor.config.json`**：按 REPORT-FIX-001 工单边界，本次同样**仅**修订文档，**不**在项目根落盘
- **完成报告**：[templates/Codex完成报告/W-ANTISLOP-SCAN-001-REPORT-FIX-002-完成报告.md](../templates/Codex完成报告/W-ANTISLOP-SCAN-001-REPORT-FIX-002-完成报告.md)；[W-ANTISLOP-SCAN-001-REPORT-FIX-001-完成报告.md](../templates/Codex完成报告/W-ANTISLOP-SCAN-001-REPORT-FIX-001-完成报告.md) REPORT-FIX-001 仍存

---

## 最近变更（W-ANTISLOP-SCAN-001-REPORT-FIX-001）

- **修正**：[backend-doctor-scan-report.md](backend-doctor-scan-report.md) 统计口径（修订版）：**项目自身 864 = A4 + B109 + C565 + D186**；外加 `community-site/` 桶 32、venv 桶 14,767；总计 15,663
- **修正原因**（首版报告错误）：
  - 首版"项目自身 278"是 `is_real_source` 过滤有 bug（误把 `tailwindcdn.js` 与 `community-site/` 部分计入），修订版按 4 个独立桶（project / community-site / venv / docs）严格分离
  - 首版"A=2"是合并 2 个规则（`ci/github-action-unpinned` + `supply-chain/unpinned-github-action`）在同一行的不同 finding；修订版拆为 4 条 finding（2 行 × 2 规则）
  - 首版"B=78"漏算 31 条（`security/sensitive-logging` 8 条翻译表/测试桩 + `agent/swallowed-error` 1 条）；修订版把翻译表/测试桩**降级**回 B 复核池（非 C）以确保逐项确认
- **新增** §11「推荐 `backend-doctor.config.json`」（仅规划，不落盘）：`ignore.files`（venv / tailwindcdn.js / build/... 9 项）+ `ignore.rules`（`security/hardcoded-secret`、`node/dead-export`）；预期复测 finding 864→约 114，`--score` 回升 50-70
- **后续工单登记**（在 [工单列表.md](工单列表.md) 顶部）：
  - **`W-DOCTOR-IGNORE-001`**（待办，高优）：落地配置；前置 = 本次报告收尾
  - **`W-CI-DOCTOR-001`**（待办，中优）：`.github/workflows/ci.yml` pin GitHub Actions 到 SHA + 评估接 `backend-doctor --ci`；前置 = `W-DOCTOR-IGNORE-001` 完成
- **A 类独立成节**：`.github/workflows/ci.yml:15` 与 `:18` 两个 Actions（`actions/checkout@v4`、`actions/setup-python@v5`）未 pin SHA——按 GitHub Security Hardening 指南应 pin 到 immutable commit SHA。**仅登记，不在本次工单修改**
- **未执行**：`--fix-safe --yes` / `--fix-guided --yes` / `--ci` / `--min-score` / `--max-critical` / `--max-errors` / `--fail-on`
- **未改动**：`app/`、`web/`、`main.py`、`tests/`、`scripts/`、`requirements*.txt`、`pyproject.toml`、`package*.json`、`.github/`、`AGENTS.md`、所有 git 跟踪的 Python / JS / CSS / HTML 业务源文件
- **未落盘 `backend-doctor.config.json`**：按用户要求仅规划内容，落盘由 `W-DOCTOR-IGNORE-001` 单独授权
- **完成报告**：[templates/Codex完成报告/W-ANTISLOP-SCAN-001-REPORT-FIX-001-完成报告.md](../templates/Codex完成报告/W-ANTISLOP-SCAN-001-REPORT-FIX-001-完成报告.md)；[W-ANTISLOP-SCAN-001-完成报告.md](../templates/Codex完成报告/W-ANTISLOP-SCAN-001-完成报告.md) 首版仍存

---

## 最近变更（W-ANTISLOP-SCAN-001，首版）

- **工具**：`@zypherhq/backend-doctor` v0.1.0（npm 全局安装因 `EPERM` 失败，回退 `npx --yes`）
- **扫描**：完整项目根（`E:\test\danmu`），149.8 秒，2,184 个文件，**15,663 finding**
- **评分**：`--score = 0`，label `Critical`，cap = 39（`raw secret detected in committed project files`）
- **分布**：critical 15,306 / error 53 / warning 171 / info 133
- **首版项目自身 finding（278）分类已被修订版（864）取代**；见 [W-ANTISLOP-SCAN-001-REPORT-FIX-001](#最近变更w-antisop-scan-001-report-fix-001) 节
- **产物**：`reports/backend-doctor.json`（25,886,127 字节）、`reports/backend-doctor.sarif`（12,291,311 字节，SARIF 2.1.0）
- **结论（首版）**：`--score = 0` 主要由误报驱动（`security/hardcoded-secret` 规则对 Python 项目不友好），**不**建议立刻接 `--ci --min-score 80`；应先开 `W-DOCTOR-IGNORE-001` 落地忽略配置，**再**评估接 CI
- **完成报告（首版）**：[templates/Codex完成报告/W-ANTISLOP-SCAN-001-完成报告.md](../templates/Codex完成报告/W-ANTISLOP-SCAN-001-完成报告.md)

---

## 最近变更（BUG-02-HIGH）

- **竞态条件修复**：`app/config_store.py` `set_api_key` 方法将锁提前到 `_fernet` 检查之前，避免并发修改导致加密/存储不一致。
- **除零错误修复**：`app/danmu_engine.py` `_pick_track` 方法添加 `total == 0` 防护，避免弹幕引擎崩溃。
- **输入验证加固**：`app/model_providers.py` `validate_model_config` 函数为 `modelId` 添加正则验证 `^[a-zA-Z0-9_./-]+$`，防止注入。
- **静默数据丢失修复**：`app/memory/store.py` `update_from_visual_result` 方法添加代际不匹配警告日志。
- **资源泄漏修复**：`app/ai_client.py` `close` 方法添加日志确认清理完成。
- **空引用异常修复**：`app/mic_service.py` `is_running` 方法添加 None 检查。
- **翻译**：`app/translations_settings.py` 添加 `custom_model.error_model_id_invalid` 中英文翻译键。
- **测试**：定向 123 passed；boundary_guard PASS。
- **未改动**：`main.py`、`app/main_*mixin.py`、`app/overlay.py`、`web/static/`。
- **完成报告**：[templates/Codex完成报告/BUG-02-HIGH-完成报告.md](../templates/Codex完成报告/BUG-02-HIGH-完成报告.md)

---

## 最近变更（W-SECURITY-001/002/003）

- **W-SECURITY-001**：`/api/diagnostics` 和 `/api/diagnostics/events` 添加 Bearer Token 认证保护。
- **W-SECURITY-002**：WebSocket 首次消息认证机制，Token 不再仅通过 URL query 参数传递，保留向后兼容。
- **W-SECURITY-003**：社区注册守卫 Edge Function CORS Origin 从通配符 `*` 改为环境变量 `COMMUNITY_CORS_ORIGIN` 配置。
- **测试**：diagnostics 11 passed、websocket 5 passed；boundary_guard PASS。
- **未改动**：`main.py` 主链路、`app/overlay.py`、`app/danmu_engine.py`。
- **完成报告**：[templates/Codex完成报告/W-SECURITY-001-完成报告.md](../templates/Codex完成报告/W-SECURITY-001-完成报告.md)、[W-SECURITY-002-完成报告.md](../templates/Codex完成报告/W-SECURITY-002-完成报告.md)、[W-SECURITY-003-完成报告.md](../templates/Codex完成报告/W-SECURITY-003-完成报告.md)

---

## 最近变更（W-FORMULA-DISPLAY-NOTRUNC-001）

- **公式化弹幕上屏**：`normalize_danmu_display_text` 经 `is_formula_danmu_text` 对自定义库与烂梗库句子完整展示；AI 弹幕仍受 `danmu_max_chars` 截断。
- **烂梗采集**：`MemeBarrageService` 入库不再截断，改用 `is_overlay_safe(max_chars=None)`。
- **新增**：`config_store.meme_barrage_library_contains_text`、`danmu_pool.is_formula_danmu_text`。
- **测试**：定向 87 passed；全量 1053 passed, 5 skipped。
- **未改动**：`main.py`、`app/personae.py`、`app/ai_client.py`。
- **完成报告**：[reports/W-FORMULA-DISPLAY-NOTRUNC-001-completion-report.md](../archive/completion-reports/W-FORMULA-DISPLAY-NOTRUNC-001-completion-report.md)

---

## 最近变更（错误反馈增强 + 空 Endpoint 校验）

- **错误反馈**：弹窗新增补充说明 / 联系方式；运行概览 error 横幅「反馈此问题」可手动重开（跳过去重）；Supabase `error_reports` 新增 `user_note`、`contact`（migration `008`）。
- **空 Endpoint**：保存配置与 `start()` 前校验全局 API 地址；`resolve_request_credentials` 空地址返回 `None`，避免 httpx missing protocol。
- **文档**：`docs/IDE_AGENT_RULES.md` §9（Supabase 排障规范）、`docs/WEB_CONSOLE.md` 错误反馈小节、`supabase/README.md`。
- **未改动**：`main.py` 主链路、`app/overlay.py`、`app/danmu_engine.py`。

---

## 最近变更（烂梗公式化）

- **公式化弹幕库**拆为 Web 双 Tab：烂梗公式化 / 自定义公式化（后者行为不变）。
- **新增**：`app/meme_barrage/*`（远程 API、本地库、展示队列、AI 筛选）、`app/main_meme_mixin.py`、`app/web_api/meme_barrage.py`；`config.db` 表 `meme_barrage_library`。
- **API**：`GET/PUT /api/meme-barrage/meta|settings`、`GET /api/meme-barrage/tags`、`POST /api/meme-barrage/clear`；`/api/status.meme_barrage` 含库计数与队列深度。
- **运行时**：`_meme_collect_timer` + `_meme_display_timer` 独立于 `reply_buffer`；AI 识别失败降级为候选前 N 条。
- **测试**：`tests/test_meme_barrage_api.py`、`test_meme_barrage_runtime.py`、`test_meme_barrage_ai.py`。

---

## 最近变更（PET-014）

- **app/pet/pet_window.py**：指令框隐藏时窗口高度贴合 sprite（去掉常驻 +40px）；paintEvent 每帧 `CompositionMode_Clear`；补全 `WA_NoSystemBackground` / `setAutoFillBackground(False)`；Win32 `WS_EX_LAYERED` 移至 `show()` / `showEvent` 后。
- **tests/test_pet_window_transparency.py**：窗口几何契约 5 项。
- **未改动**：`main.py`、`app/overlay.py`、`app/danmu_engine.py`、主链路。
- **完成报告**：[reports/pet-014-completion-report.md](../archive/completion-reports/pet-014-completion-report.md)

---

## 最近变更（W-AI-BUTLER-REMOVE-001）

- **删除**：`app/web_api/ai_butler.py`、`web/static/modules/content-ai-butler.js`、`tests/test_ai_butler.py`
- **app/web_api/routes.py**：移除 `POST /api/ai-butler/chat` 及 `AiButlerChatPayload`
- **web/static/**：侧栏与 `#page-ai-butler` 页面、`.ai-butler-*` CSS、`content-pages.js` / `app.js` 绑定；`index.html` 已 rebuild
- **docs**：`WEB_CONSOLE.md`、`MAIN_PIPELINE.md` 移除 AI 管家条目
- **未改动**：`main.py`、`app/main_*mixin.py`、`app/overlay.py`、`app/pet/*`、主链路
- **旧配置**：无 AI 管家专用 config 键，无需迁移
- **完成报告**：[reports/W-AI-BUTLER-REMOVE-001-completion-report.md](../archive/completion-reports/W-AI-BUTLER-REMOVE-001-completion-report.md)

---

## 经验沉淀（bililive_dm 桥接 Spike，无主仓代码变更）

- **范围**：独立目录 `E:/test/bililive_dm_bridge_test`；`E:/test/danmu` **未**改业务代码。
- **结论**：`ExternalDanmakuBridge` + `send_danmuai_to_bililive.py` 端到端验证通过；弹幕姬侧边栏可显示 DanmuAI 标准化文本。
- **关键经验**：弹幕姬「測試效果」≠ HTTP 桥测试；桥接 token 为 `danmuai-local-test`（端口 **18766**），与 Web 控制台 **18765** session token 无关；裸 POST 会 `invalid_token`。
- **后续**：主程序自动推送须单独工单（建议挂点 `_display_danmu_text`、配置键 `bililive_bridge_*`）。
- **报告**：[reports/bililive-dm-bridge-spike-report.md](../archive/completion-reports/bililive-dm-bridge-spike-report.md)

---

## 最近变更（W-DANMU-POOL-BUILTIN-REMOVE-001）

- **app/danmu_pool.py**：仅自定义公式化句库；移除内置 JSON/bootstrap 加载与 `danmu_pool_enabled_from_config`。
- **app/danmu_pool_overlay.py**（新）：`is_overlay_safe` 自 `scripts/extract_danmu_pool.py` 迁出。
- **app/web_api/danmu_pool.py**、**routes.py**：meta/settings 无 `builtin_*`；追加自定义句不再 `merged_duplicate` 查内置。
- **app/config_defaults.py**：移除 `danmu_pool_enabled` 默认键。
- **web/static/**：公式化弹幕库页移除内置开关；`index.html` 已 rebuild。
- **data/**：删除 `danmu_pool_zh.json`、`danmu_pool_zh_bootstrap.txt` 及生成脚本。
- **DanmuAI.spec**、**scripts/build_exe.ps1**：不再打包内置弹幕库数据。
- **tests**：池/兜底相关用例改为 `danmu_pool_use_custom` + 自定义句；`FakeConfig` 增加 `get/set_custom_danmu_pool`。
- **未改动**：`main.py`、`app/main_*mixin.py`。
- **本地验证**：定向 pytest **40 passed**；`boundary_guard` **PASS**；全量 **1027 passed, 1 failed**（`test_live_overlay` 范围外既有失败）。
- **完成报告**：[reports/W-DANMU-POOL-BUILTIN-REMOVE-001-completion-report.md](../archive/completion-reports/W-DANMU-POOL-BUILTIN-REMOVE-001-completion-report.md)

---

## 最近变更（W-PROVIDER-NAMING-001）

- **app/model_providers.py**：协议型预设 `label_zh`/`label_en` 改名；mic 拦截提示改用「模型配置档案」。
- **app/translations_settings.py**：`custom_model.*`、`config.error_model_id_reserved_for_custom` 中英文同步。
- **app/web_api/ai_butler.py**、**app/mic_test_send.py**：产品指引文案同步。
- **web/static/modules/settings-providers.js**：「手动填写」置底；麦克风 tab `MIC_LABEL_SUFFIX` 能力后缀。
- **web/static/**：hints、settings、custom-models、model-catalog、partials；`index.html` 已 rebuild。
- **tests**：`test_web_settings_ui_provider_naming_unified`；`test_model_selection` 翻译断言更新。
- **未改动**：`main.py`、`app/main_*mixin.py`、`/api/custom-models*` 路由、运行时逻辑、`app/tts_providers.py`。
- **本地验证**：定向 pytest **59 passed**；全量 **1035 passed, 1 failed**（`test_live_overlay` 范围外）。
- **完成报告**：[reports/W-PROVIDER-NAMING-001-completion-report.md](../archive/completion-reports/W-PROVIDER-NAMING-001-completion-report.md)

---

## 最近变更（W-DANMU-READ-AUDIT-003）

- **app/tts_providers.py**：文本-only chat 响应时提示「不支持读弹幕 TTS 音频输出」；抽取 `_decode_audio_wav_from_body`。
- **app/danmu_read_service.py**：`_on_tick` 内 `resolve_tts_config` 异常隔离；GET 导出补充 `model_id`。
- **app/web_api/danmu_read.py**：PUT/probe 接受 `custom_endpoint`/`custom_model_id` 别名。
- **app/web_api/routes.py**：读弹幕 Pydantic payload 增加别名字段。
- **web/static/partials/settings.html**：读弹幕页说明 TTS 与聊天模型区别；`index.html` 已 rebuild。
- **tests**：`test_danmu_tts.py`、`test_danmu_read_api.py` 新增 4 项回归。
- **未改动**：`main.py`、`app/main_*mixin.py`、麦克风模块、主链路。
- **本地验证**：定向 pytest **23 passed**。
- **完成报告**：[templates/Codex完成报告/W-DANMU-READ-AUDIT-003-完成报告.md](../templates/Codex完成报告/W-DANMU-READ-AUDIT-003-完成报告.md)

---

## 最近变更（W-MIC-AUDIT-002）

- **app/model_providers.py**：`model_supports_mic_audio` 接入 catalog、`supportsMic` 声明与 `ProviderCapabilities`；新增 `mic_audio_unsupported_message`。
- **app/model_catalog.py**：`catalog_model_supports_mic`。
- **app/mic_*.py**：测试录音 `QEventLoop` 非阻塞等待；`MicService.try_snapshot_pcm_ms` 门面。
- **app/web_api/custom_models.py**：自定义模型持久化 `supportsMic`。
- **web/static/**：麦克风开麦来源 banner；自定义模型「支持麦克风」勾选；`evaluateMicAudioSupported` 与后端对齐。
- **未改动**：`main.py`、`app/main_*mixin.py`、读弹幕/TTS、主链路调度。
- **本地验证**：定向 mic/catalog pytest **79 passed**；全量 **1030 passed**（2 项既有失败，范围外）；`boundary_guard` PASS。
- **完成报告**：[templates/Codex完成报告/W-MIC-AUDIT-002-完成报告.md](../templates/Codex完成报告/W-MIC-AUDIT-002-完成报告.md)

---

## 最近变更（W-MODEL-PROBE-001）

- **app/model_providers.py**：`normalize_endpoint` 剥离 `/chat/completions`、`/responses` 后缀，避免完整 URL 双重拼接。
- **app/web_api/custom_models.py**：新增 `resolve_probe_credentials`；保存/探测共用 `_normalize_payload`；掩码 Key 无 existing 时解析为空。
- **app/web_api/routes.py**：`/api/custom-models/probe` 先解析凭据再探测；新增 `CustomModelProbePayload.index`。
- **web/static/modules/settings.js**：探测请求附带 `modelEditIndex`。
- **tests**：`test_model_providers.py`、`test_web_custom_models.py` 新增 8 项回归。
- **未改动**：`main.py`、`app/main_*mixin.py`、`app/ai_client_requests.py`、全局 `/api/probe` 语义。
- **本地验证**：定向 pytest **72 passed**；全量 **1025 passed**（1 项既有 `test_live_overlay` 失败，范围外）。
- **完成报告**：[templates/Codex完成报告/W-MODEL-PROBE-001-完成报告.md](../templates/Codex完成报告/W-MODEL-PROBE-001-完成报告.md)

---

## 最近变更（PET-013）

- **app/pet/pet_window.py**：拖动状态机（`jumping` → `running-left`/`running-right` → `waving` → mapper）；松手惯性（摩擦 0.88、最长 900ms）；`availableGeometry` 钳制；拖动/惯性优先于 Agent 动画。
- **app/pet/pet_assets.py**：`DANMUAI_TO_PETDEX_STATE` 增加 `running-right`/`running-left`/`jumping`/`waving` 直通别名。
- **tests/test_pet_window_drag.py**（新增）：9 项拖动/惯性/优先级契约测试。
- **未改动**：`pet_animation_mapper.py`、`main.py`、AI 请求链路。
- **本地验证**：定向 pytest **36 passed**
- **完成报告**：[reports/pet-013-completion-report.md](../archive/completion-reports/pet-013-completion-report.md)

---

## 最近变更（PET-012）

- **app/pet/pet_assets.py**：新增 `PetDexStateSpec` 9 行规格表（PetDex Desktop：8 列×9 行，192×208）；`DANMUAI_TO_PETDEX_STATE` 别名（`wave→waving`、`run→running`、`jump→jumping`）；`frame_rect` / `frame_count(state)` / `state_duration_ms` / `state_frame_interval_sec` 经别名解析取 row、frames、duration。
- **app/pet/pet_window.py**：移除全局 `_FRAME_DT = 1/9`；按当前状态 `duration_ms / frames` 换帧。
- **tests/test_pet_assets.py**：`test_validate_builtin_dimensions` 锁定 8×9；新增 review/run/wave/jump 行号契约与 duration 测试。
- **未改动关键区域**：`main.py`、`pet_animation_mapper.py`、`pet_prompt.py`、AI 请求链路、Web API。
- **本地验证**：定向 pytest（pet_assets + mapper + command + inject）：**27 passed**
- **完成报告**：[reports/pet-012-completion-report.md](../archive/completion-reports/pet-012-completion-report.md)

---

## 最近变更（PET-011）

- **tests/test_pet_assets.py**：`test_validate_builtin_dimensions` 从 `cols >= 1 and rows >= 1` 收紧为 **`cols == 8`、`rows == 9`**，锁定默认 `spritesheet.webp` 网格与 `PET_FRAME_W/H`（192×208）美术契约。
- **tests/test_pet_animation_mapper.py**（新增）：5 项契约测试覆盖 `resolve_base_animation` 的 idle / review（`ai_in_flight`、`_is_generating`）/ run，以及 `resolve_pet_animation_hint` 的 one_shot 优先。
- **未改动关键区域**：`main.py`、`app/pet/*` 业务代码、AI 请求链路、桌宠 prompt 注入、桌宠窗口动画参数（idle 帧率等）。
- **本地验证**：
  - 定向 pytest（`tests/test_pet_assets.py` + `tests/test_pet_animation_mapper.py`）：**14 passed**
  - `python scripts/boundary_guard.py`：**PASS**
- **完成报告**：[reports/pet-011-completion-report.md](../archive/completion-reports/pet-011-completion-report.md)
- **后续建议**：idle 闪烁观感优化另开 **PET-010**（体验参数，与契约测试分离）。

---

## 最近变更（PET-009）

- **DanmuAI.spec**：`datas` 追加 `(str(root / "data" / "pet" / "default"), "data/pet/default")`；打包后 `resource_path("data", "pet", "default", "pet.json")` 在 `sys._MEIPASS` 下也能被 `BUILTIN_PET_DIR` 解析到，桌宠窗口不再「宠物加载失败」。
- **app/main_lifecycle_mixin.py**：`_init_startup_services` 末尾追加一次主线程 `_sync_pet_window_visibility()`（仅在 `pet_window` 已构造时），复用 `app.pet.pet_facade.sync_pet_window_visibility` 既有 façade；`config_changed` 信号未连接阶段也能让 `pet_enabled=1 + pet_visible=1` 的桌宠自动显示，后续 web / ConfigService 修改仍由 `_on_config_changed` 路径兜底。
- **tests/test_bundle_paths.py**：新增 `test_danmuai_spec_includes_pet_default_assets` + `test_resource_path_pet_default_pet_json_and_spritesheet` 静态回归。
- **tests/test_pet_assets.py**：新增 4 个 sync 行为回归（show / hide when disabled / hide when visible=0 / noop when window missing）。
- **未改动关键区域**：`app/pet/pet_facade.py`、`app/pet/pet_assets.py`、`app/pet/pet_state.py`、`app/pet/pet_command_service.py`、`app/pet/pet_prompt.py`、`app/main_display_mixin.py`、`main.py`、`app/web_api/*`、`app/overlay.py`、`app/danmu_engine.py`、`web/static/*`。
- **本地验证**：
  - 桌宠 + bundle 定向 pytest（`tests/test_bundle_paths.py` + `tests/test_pet_assets.py` + `tests/test_pet_command_service.py` + `tests/test_pet_inject.py` + `tests/test_pet_prompt.py` + `tests/test_web_routes.py`）：**64 passed**
  - 全量 pytest：**998 passed, 5 skipped, 1 failed**；失败项 `tests/test_live_overlay.py::test_broadcast_failure_does_not_break_enqueue` 已在 stash 后于 da32d63 复现，确认为范围外既有失败
  - `python scripts/boundary_guard.py`：**PASS**
- **完成报告**：[reports/pet-009-completion-report.md](../archive/completion-reports/pet-009-completion-report.md)

---

## 最近变更（PET-001～PET-008 桌宠）

- **Web**：侧边栏「桌宠」页（`page-pet`、`app-pet-page.js`）；`GET/POST /api/pet/*`。
- **Qt**：`app/pet/`（`PetWindow`、`PetCommandService`、PetDex 素材加载）；独立于 `danmu_render_mode`。
- **主链路**：`main.py` `_trigger_api_call` 在调度通过后消费桌宠指令并追加 `user_pt`（无独立 AI）。
- **素材**：`data/pet/default/`（自 `zip/` 迁入）；`zip/` 已删除。
- **本地验证**：
  - 桌宠定向 pytest：**17 passed**
  - 全量 pytest：**992 passed, 5 skipped, 1 failed**（`test_live_overlay.py::test_broadcast_failure_does_not_break_enqueue`，范围外既有失败）
  - `python scripts/boundary_guard.py`：**PASS**
- **完成报告**：[reports/pet-feature-completion-report.md](../archive/completion-reports/pet-feature-completion-report.md)

---

## 最近变更（W-FP-V2-002）

- **app/config_defaults.py**：新增 `migrate_legacy_display_mode_to_render_mode()`；`resolve_danmu_render_mode()` 仅读 `danmu_render_mode`；从 `CONFIG_DEFAULTS` 移除 `display_mode`。
- **app/config_store.py**：`__init__` 在 `seed_config_defaults` **之前**调用写回迁移（避免 seed 先落 `scrolling` 盖掉遗留 `display_mode`）。
- **删除 V1 壳**：`app/floating_panel.py` 已不存在；`main_lifecycle_mixin.py` 移除 `floating_panel` 别名。
- **运行态 / API**：`runtime_state.py`、`status_snapshot.py`、`WebStatusSnapshot` 移除 `display_mode`；`_floating_panel_metrics` 仅查 `floating_panel_overlay`。
- **Web**：`settings-core.js`、`status.js` 仅认 `danmu_render_mode`。
- **boundary_guard**：`ALLOWED_DIRECT_ATTRS` 移除 `floating_panel`。
- **tests**：迁移写回单测、`test_status_floating_panel` 断言无 `display_mode`。
- **本地验证**：
  - 定向 pytest：**37 passed**
  - 全量 pytest：**975 passed, 5 skipped, 1 failed**（`tests/test_live_overlay.py::test_broadcast_failure_does_not_break_enqueue`，范围外，非本工单引入）
  - `python scripts/boundary_guard.py`：**PASS**

---

## 最近变更（IDE / Coding Agent 执行规范）
- **新增统一执行规范入口。**：新增 `docs/IDE_AGENT_RULES.md`，面向 IDE、Codex、Cursor、Claude Code、Windsurf 等代码执行工具，强制固定执行顺序为“先读边界 -> 再做工单 -> 再写代码 -> 再写完成报告 -> 最后反查并更新项目说明文件”。
- **明确必读入口与模板。**：在规范中集中列出 `README.md`、`AGENTS.md`、`docs/ai-project-context.md`、`docs/CONTRIBUTING_ARCHITECTURE.md`、`docs/MAIN_PIPELINE.md`、`docs/WEB_CONSOLE.md`、`docs/RUNTIME_STATE.md`、`docs/BOUNDARY_GUARD.md`、维护者登记表、工单模板与完成报告模板，要求所有代码修改前先读再做。
- **新增任务后置检查要求。**：明确要求每次任务完成后必须输出独立完成报告，并反查是否需要同步更新 `AGENTS.md`、`README.md`、架构文档、规则文档与模板文件；若无需更新，也必须在完成报告中写出固定原文说明。
- **文档索引同步。**：已在 `docs/README.md` 的 Start here 区域增加 `IDE_AGENT_RULES.md` 入口，避免新规范文件成为“隐形文档”。
- **未改动关键区域**：本轮未修改 `main.py`、`app/`、`web/static/`、`tests/`、`scripts/`、构建配置或任何运行态逻辑，仅新增和同步文档入口。
- **本地验证**：
  - `git diff --name-only`
  - 本项目仍无 docs 专用自动化校验命令，需要人工补充

---

## 最近变更（文档校准 / 边界与限制文件重写）
- **重写核心入口文档**：已按当前代码事实重写 `docs/ai-project-context.md`、`docs/CONTRIBUTING_ARCHITECTURE.md`、`docs/MAIN_PIPELINE.md`、`docs/WEB_CONSOLE.md`、`docs/RUNTIME_STATE.md`、`docs/runtime-state-map.md`、`docs/BOUNDARY_GUARD.md`，删除旧的阶段迁移叙事、过期行号、旧线程模型描述，统一到当前 `main.py + app/main_*mixin.py`、`RequestScheduler` / `RequestTimingService`、pywebview 子进程、Web façade 与 snapshot builder 的实际边界。
- **同步修正 AGENTS 附录事实**：更新 `AGENTS.md` 附录中的架构树、线程/进程模型与核心模块速查，补入 `main_lifecycle_mixin` / `main_request_context_mixin` / `main_display_mixin` / `main_mic_mixin`，并明确 pywebview 当前是子进程而不是旧文档里的“pywebview 线程”。
- **未改动关键区域**：本轮没有修改 `main.py`、`app/` 业务实现、`web/static/` 前端逻辑、测试代码、Boundary Guard 规则实现，也没有变更任何用户可见行为；仅修正文档以匹配当前项目事实。
- **本地验证**：
  - `git diff --name-only`
  - 本项目仍无 docs 专用自动化校验命令，需要人工补充。

---

## 最近变更（超 800 行治理 / TASK-main-lifecycle-error-boundary-split-4）
- **main.py + app/main_lifecycle_mixin.py**：将 `DanmuApp` 的生命周期、错误处理和启动编排从 `main.py` 迁到新的 `DanmuAppLifecycleMixin`，包括 `__init__` 中的 Web/Tray/Hotkey/WebConsole/FloatingPanel 启动辅助、`_on_config_changed`、`_on_ai_error`、`start`、`_flush_session_runtime_to_lifetime`、`stop`、`toggle`、`quit`。
- **主链路冻结入口保持不变**：`_trigger_api_call`、`_on_ai_reply`、`_consume_reply_queue` 仍留在 `main.py`；`QTimer`、`QThreadPool`、`QPixmap`、`reply_buffer`、`ai_in_flight`、`_pending_request_meta` 的所有权仍由 `DanmuApp` 持有，没有新增平行调度字段或线程模型改动。
- **边界未越界**：未改 `app/web_api/*`、`app/overlay.py`、`app/danmu_engine.py`、`app/application/request_scheduler.py`、`app/application/request_timing_service.py` 的行为与所有权。
- **行数复查**：`main.py` 由 `1125` 降至 `586`；新增 `app/main_lifecycle_mixin.py` `565` 行，二者都低于 800 行。
- **本地验证**：
  - `python scripts/boundary_guard.py`
  - `python -m pytest tests/test_request_scheduling.py tests/test_boundary_guard_web_rules.py tests/test_boundary_guard_runtime_rules.py tests/test_boundary_guard_request_rules.py tests/test_boundary_guard_diagnostics_rules.py -q`
  - `python -m pytest tests/ -q`
  - `python main.py --web-browser`

---

## 最近变更（超 800 行治理 / TASK-bug-audit-p2-doc-split）
- **docs/bug-audit/P2-MEDIUM.md + 4 个 P2 分卷文档**：将原 `P2-MEDIUM.md` 从 829 行改为索引页，新增 `P2-rendering-and-performance.md`、`P2-state-and-logging.md`、`P2-scheduling-and-resources.md`、`P2-maintainability-and-dead-code.md` 4 个主题分卷；原入口保留总览、分卷目录、简目录和后续归档规则，避免历史链接完全失效。
- **文档结构保持可追踪**：原 BUG 编号、状态、证据、修复方向和验证建议全部保留在分卷中，没有改动审计结论本身，也没有复制出多份重复索引。
- **计划文档同步**：已更新 `docs/refactor-line-limit-plan/00-scan-result.md` 与 `docs/refactor-line-limit-plan/phase-3-low-risk/README.md`，并记录 `P3-LOW.md` 可复用相同的“索引页 + 主题分卷”结构。
- **未改动关键区域**：未修改任何业务代码、测试代码、运行态字段、Web/API 路由或 Boundary Guard 规则。
- **本地验证**：
  - `git diff --name-only`
  - 未发现 docs 专用校验命令，需要人工补充。

---

## 最近变更（超 800 行治理 / TASK-danmu-pool-data-format-governance）
- **data/danmu_pool_zh-governance.md + data/README.md**：为 `data/danmu_pool_zh.json` 补充正式治理结论与维护说明，明确当前文件属于结构化数据资产，采用正式豁免，不做机械拆分，也不做元数据/语料分离；保留生成入口、字段结构、维护规则和风险边界。
- **计划文档同步**：已更新 `docs/refactor-line-limit-plan/00-scan-result.md` 与 `docs/refactor-line-limit-plan/phase-3-low-risk/README.md`，避免后续执行者重复评估同一数据文件。
- **未改动关键区域**：未修改 `data/danmu_pool_zh.json` 内容与结构，未修改 `app/danmu_pool.py`、`app/web_api/danmu_pool.py`、`main.py`、`tests/`、`scripts/` 的运行逻辑。
- **本地验证**：
  - `python -m pytest tests/ -q`
  - `git diff --name-only`
  - 未发现该数据治理工单专用的独立自动化命令，需要人工补充。

---

## 最近变更（超 800 行治理 / TASK-floating-panel-tests-split）
- **tests/test_floating_panel_render.py + test_floating_panel_config.py + test_floating_panel_fonts.py + test_floating_panel_integration.py + tests/conftest.py**：将原 `tests/test_floating_panel.py` 按 `render / config / fonts / integration` 四个主题拆分；共享的最小 `DanmuApp` 装配、方法绑定和 `SpyOverlay` 收敛到 `tests/conftest.py`，继续复用现有 `tests/fakes.py`，没有复制假对象。
- **测试边界保持不变**：未修改任何业务实现文件，未整理非浮窗测试，monkeypatch 目标路径、Qt 事件循环时序和原断言语义保持不变。
- **行数复查**：原 `tests/test_floating_panel.py` 已由多文件替代；新增 `test_floating_panel_render.py` `145` 行、`test_floating_panel_config.py` `122` 行、`test_floating_panel_fonts.py` `62` 行、`test_floating_panel_integration.py` `518` 行，均低于 800 行。
- **本地验证**：
  - `python -m pytest tests/test_floating_panel_render.py tests/test_floating_panel_config.py tests/test_floating_panel_fonts.py tests/test_floating_panel_integration.py -q`：34 passed
  - `python -m pytest tests/ -q`：992 passed, 5 skipped

---

## 最近变更（超 800 行治理 / TASK-warm-tokens-css-bundle-split）
- **web/static/warm-tokens.css + warm-tokens-base.css + warm-tokens-layout.css + warm-tokens-components.css + warm-tokens-pages.css + warm-tokens-dark.css**：将控制台样式从单文件拆成 5 个职责层，`warm-tokens.css` 退化为聚合入口，按 `base -> layout -> components -> pages -> dark` 顺序通过 `@import` 加载，保留原始单 `<link>` 入口与覆盖顺序。
- **前端边界保持不变**：未修改现有 class 语义、未修改页面 DOM 结构、未切换 CSS 框架、未调整 `index.html` 样式入口路径，也未改动 `app.js` / `modules/*.js` 的依赖契约。
- **行数复查**：`web/static/warm-tokens.css` 由 `1294` 降至 `20`；新增 `warm-tokens-base.css` `155` 行、`warm-tokens-layout.css` `96` 行、`warm-tokens-components.css` `265` 行、`warm-tokens-pages.css` `621` 行、`warm-tokens-dark.css` `153` 行，均低于 800 行。
- **本地验证**：
  - `python -m pytest tests/test_console_theme.py -q`：6 passed
  - `python -m pytest tests/ -q`：992 passed, 5 skipped
  - `python main.py --web-browser`：启动成功并返回，未见立即崩溃

---

## 最近变更（超 800 行治理 / TASK-index-html-fragmentation-split）
- **web/static/index.template.html + web/static/partials/*.html + web/static/build_index_html.py + web/static/index.html**：将控制台静态骨架按职责拆成模板源、侧栏、运行概览、设置页、内容页、模态框 5 个 partial，并新增最小生成脚本 `python web/static/build_index_html.py`；运行时 `index.html` 仍保持单入口完整静态 DOM，不改 `app.js` 入口事实。
- **前端边界保持不变**：未修改 `web/static/app.js`、`web/static/modules/*.js` 的导出契约，未修改 `app/web_api/*` 的 API 路径与返回结构，未修改现有 DOM id、`data-*` 属性名、脚本加载顺序和用户可见行为。
- **行数复查**：`web/static/index.html` 由 `1269` 降至 `97`；新增 `index.template.html` `97` 行、`build_index_html.py` `44` 行、`partials/sidebar.html` `64` 行、`partials/overview.html` `199` 行、`partials/settings.html` `511` 行、`partials/content-pages.html` `280` 行、`partials/modals.html` `123` 行，均低于 800 行。
- **本地验证**：
  - `python -m pytest tests/test_bundle_paths.py -q`：17 passed
  - `python -m pytest tests/test_web_server.py -q`：19 passed
  - `python -m pytest tests/ -q`：992 passed, 5 skipped
  - `python main.py --web-browser`：启动成功并返回，未见立即崩溃

---

## 最近变更（超 800 行治理 / TASK-content-pages-announcements-feedback-butler-split）
- **web/static/modules/content-pages.js + content-announcements.js + content-feedback.js + content-ai-butler.js**：将公告已读状态与 badge/banner、公告列表页、反馈配额与提交、AI 管家对话与配置建议逻辑从 `content-pages.js` 拆出；`content-pages.js` 保留依赖注入、reward modal 和兼容导出层。
- **前端边界保持不变**：未修改 `window.DanmuSupabase` 套壳、未修改 `POST /api/ai-butler/chat`、未修改 `GET/PUT /api/announcements-read-state`，也未改变现有 DOM id、内容页行为和 `app.js` 对 `content-pages.js` 的导出依赖。
- **行数复查**：`web/static/modules/content-pages.js` 由 `829` 降至 `107`；新增 `content-announcements.js` `468` 行、`content-feedback.js` `76` 行、`content-ai-butler.js` `250` 行，均低于 800 行。
- **本地验证**：
  - `python -m pytest tests/test_bundle_paths.py -q`：17 passed
  - `python -m pytest tests/test_web_server.py -q`：19 passed
  - `python -m pytest tests/ -q`：992 passed, 5 skipped
  - `python main.py --web-browser`：49 秒超时前持续运行，未见立即崩溃

---

## 最近变更（超 800 行治理 / TASK-web-app-bootstrap-orchestration-split）
- **web/static/app.js + app-error-reporting.js + app-live-overlay-panel.js + app-danmu-pool-page.js + app-persona-topic-page.js + app-update-banner.js**：将 Web 控制台入口中的错误反馈、直播弹幕层、公式化弹幕库、人格/主题页、版本更新弹窗逻辑迁出；`app.js` 保留唯一入口、初始化顺序、跨模块依赖注入与 `danmu-read` 页面编排。
- **前端边界保持不变**：未修改 `web/static/index.html` 入口脚本加载方式，未修改 `web/static/modules/transport.js`，未修改 `app/web_api/*` 的 API 路径或返回结构，也未改变现有 DOM id、页面切换顺序和用户可见行为。
- **行数复查**：`web/static/app.js` 由 `1330` 降至 `406`；新增 `app-error-reporting.js` `316` 行、`app-live-overlay-panel.js` `84` 行、`app-danmu-pool-page.js` `157` 行、`app-persona-topic-page.js` `239` 行、`app-update-banner.js` `260` 行，均低于 800 行。
- **本地验证**：
  - `node --check web/static/app.js`：PASS
  - `python -m pytest tests/test_web_server.py -q`：19 passed
  - `python -m pytest tests/ -q`：992 passed, 5 skipped
  - `python main.py --web-browser`：45 秒超时前持续运行，未见立即崩溃

---

## 最近变更（超 800 行治理 / TASK-settings-media-font-tools-split-3）

- **web/static/modules/settings.js + settings-capture-region.js + settings-compress-preview.js + settings-mic-tools.js + settings-fonts.js + settings-hints.js**：将识图区域框选、压缩预览、麦克风测试、字体工具、字段提示/tooltip 从 `settings.js` 迁出；`settings.js` 继续保留兼容导出和装配层，不改 `app.js` 入口调用。
- **前端边界保持不变**：未改 `app/web_api/capture_region.py`、`app/web_api/preview_compress.py`、`app/web_api/font_registry.py`、`app/web_api/mic_test.py`，未改现有按钮 id、上传路径、页面行为，也未改 `web/static/index.html` 的 DOM 结构。
- **行数复查**：`web/static/modules/settings.js` 由 `982` 降至 `458`；新增 `settings-capture-region.js` `119` 行、`settings-compress-preview.js` `99` 行、`settings-mic-tools.js` `67` 行、`settings-fonts.js` `94` 行、`settings-hints.js` `219` 行；`web/static/modules/` 当前 `.js` 文件全部低于 800 行。
- **本地验证**：
  - `node --input-type=module -e "import('file:///E:/test/danmu/web/static/modules/settings.js').then(() => console.log('ok'))"`：PASS
  - `python -m pytest tests/test_web_server.py -q`：19 passed
  - `python -m pytest tests/ -q`：992 passed, 5 skipped
  - `python main.py --web-browser`：44 秒超时前持续运行，未见立即崩溃

---

## 最近变更（超 800 行治理 / TASK-settings-model-catalog-split-2）

- **web/static/modules/settings.js + settings-providers.js + settings-model-catalog.js + settings-custom-models.js**：将服务商预设、模型目录 picker、自定义模型 CRUD 从 `settings.js` 迁出。新增 provider / catalog / custom-model 三个子模块，`settings.js` 继续保留原导出名和兼容层锚点，`app.js` 无需改入口调用。
- **前端边界保持不变**：未改 `web/static/index.html` 的 DOM id，未改 `web/static/app.js` 的唯一入口职责，未改 `app/web_api/custom_models.py` 与相关模型 API 路径、返回结构，也未改变设置页用户可见行为。
- **行数复查**：`web/static/modules/settings.js` 由 `1612` 降至 `982`；新增 `web/static/modules/settings-providers.js` `130` 行、`web/static/modules/settings-model-catalog.js` `417` 行、`web/static/modules/settings-custom-models.js` `101` 行；新增文件均未超过 800 行。
- **本地验证**：
  - `node --input-type=module -e "import('file:///E:/test/danmu/web/static/modules/settings.js').then(() => console.log('ok'))"`：PASS
  - `python -m pytest tests/test_web_server.py -q`：19 passed
  - `python -m pytest tests/ -q`：992 passed, 5 skipped
  - `python main.py --web-browser`：启动成功并返回，未见立即崩溃

---

## 最近变更（超 800 行治理 / TASK-settings-form-core-split-1）
- **web/static/modules/settings.js + settings-defaults.js + settings-core.js + settings-tabs.js**：将助手设置页的基础表单核心从 `settings.js` 拆出。新模块分别承载配置字段白名单与默认值缓存、collect/fill/reload/恢复默认逻辑，以及 settings tab / UI mode 状态；`settings.js` 继续保留原导出函数名作为兼容层。
- **前端边界保持不变**：未改 `web/static/index.html` 既有 DOM id、未改 `app.js` 入口职责、未改 `app/web_api/*` 路径与返回结构，也未改变设置页用户可见行为。
- **行数复查**：`web/static/modules/settings.js` 由 `2052` 降至 `1612`；新增 `web/static/modules/settings-defaults.js` 为 `160` 行、`web/static/modules/settings-core.js` 为 `269` 行、`web/static/modules/settings-tabs.js` 为 `77` 行；新增文件均未超过 800 行。
- **本地验证**：
  - `node --input-type=module -e "import('file:///E:/test/danmu/web/static/modules/settings.js').then(() => console.log('ok'))"`：PASS
  - `python -m pytest tests/ -q`：992 passed, 5 skipped
  - `python main.py --web-browser`：44 秒超时前持续运行，未见立即崩溃

---

## 最近变更（超 800 行治理 / TASK-danmu-engine-core-split）
- **app/danmu_engine.py + app/danmu_engine_dedup.py + app/danmu_engine_models.py**：将 dedup/profile 与 `DanmuItem` / `Track` 模型从 `app/danmu_engine.py` 拆出，保留 `DanmuEngine` 核心更新逻辑、`Overlay` 受控耦合，以及 `DanmuEngine`、`normalize_danmu_display_text`、dedup profile API 的兼容导出。
- **显示链路边界保持不变**：未改 `main.py` 主链路、`app/overlay.py` 渲染行为、`app/web_api/*`、SQLite schema；未新增线程、定时器、运行态字段，也未改 `_pick_track`、去重阈值、可见性统计语义。
- **行数复查**：`app/danmu_engine.py` 由 `931` 降至 `790`；新增 `app/danmu_engine_dedup.py` 为 `87` 行、`app/danmu_engine_models.py` 为 `91` 行；三者均未超过 800 行。
- **本地验证**：
  - `python -m pytest tests/test_danmu_engine.py tests/test_danmu_motion.py tests/test_danmu_dedup.py -q`：75 passed
  - `python scripts/boundary_guard.py`：PASS
  - `python -m pytest tests/ -q`：992 passed, 5 skipped

---

## 最近变更（超 800 行治理 / TASK-main-pipeline-request-context-split-3）

- **main.py + app/main_request_context_mixin.py**：将 request meta、timing、memory、密度/队列辅助从 `main.py` 迁出到新的 `DanmuAppRequestContextMixin`，包含 `_register_request_meta`、`_pop_request_meta`、`_release_inflight_for_source`、`_api_schedule_block_reason`、`_log_api_schedule`、`_consume_request_timing`、`_memory_tone_hint`、`_memory_mode`、`_memory_enabled`、`_collect_activity_observation`、`_append_scene_memory_to_user_pt`、`_record_scene_memory_display`、`_queue_capacity`、`_reply_request_id`、`_min_density_target`、`_density_right_target`、`_maybe_pool_topup`、`_estimated_reply_gap_ms`、`_reply_low_watermark`、`_empty_accel_enabled`、`_enqueue_reply_batch`、`_rtt_avg`、`_smart_cooldown_ms`、`_maybe_log_dedup_profile`。
- **主链路边界保持不变**：`_trigger_api_call`、`_on_ai_reply`、`_consume_reply_queue` 仍留在 `main.py`；`RequestScheduler` / `RequestTimingService` 所有权未变；未新增平行调度字段或 timing 副本。
- **行数复查**：`main.py` 由 `1445` 降至 `1125`；新增 `app/main_request_context_mixin.py` 为 `356` 行；未引入新的超 800 行文件。
- **未触达关键区域**：`app/application/request_scheduler.py`、`app/application/request_timing_service.py`、`app/memory/*` 业务语义、`app/web_api/*`。
- **本地验证**：
  - `python scripts/boundary_guard.py`：PASS
  - `python -m pytest tests/test_request_scheduling.py tests/test_boundary_guard_request_rules.py tests/test_boundary_guard_runtime_rules.py -q`：35 passed
  - `python -m pytest tests/test_activity_integration.py tests/test_request_scheduling.py tests/test_scene_memory.py tests/test_pool_topup.py tests/test_mic_insert.py tests/test_reply_enqueue.py tests/test_danmu_dedup.py -q`：105 passed
  - `python -m pytest tests/ -q`：992 passed, 5 skipped
  - `python main.py --web-browser`：启动后保持运行，34 秒超时前未见立即崩溃

---

## 最近变更（超 800 行治理 / TASK-main-pipeline-display-runtime-split-2）

- **main.py + app/main_display_mixin.py**：将显示辅助从 `main.py` 迁出到新的 `DanmuAppDisplayMixin`，包含 `_current_danmu_delay_sec`、`_build_live_status_snapshot`、`_publish_live_status`、`_visible_display_count`、`_right_visible_count`、`_broadcast_live_overlay_item`、`_overlay_display_enabled`、`_sync_overlay_visibility`、`_floating_panel_enabled`、`_floating_panel_debug_enabled`、`_sync_floating_panel_display_mode`、`_feed_floating_panel`、`inject_test_danmu_batch`。
- **主链路边界保持不变**：`_trigger_api_call`、`_on_ai_reply`、`_consume_reply_queue` 仍留在 `main.py`；`start()` / `stop()` 仍保留生命周期编排；`QTimer`、`QThreadPool`、`QPixmap`、`reply_buffer`、`ai_in_flight`、`_pending_request_meta` 所有权未变。
- **行数复查**：`main.py` 由 `1636` 降至 `1445`；新增 `app/main_display_mixin.py` 为 `227` 行；未引入新的超 800 行文件。
- **未触达关键区域**：`app/web_api/*`、`app/overlay.py`、`app/danmu_engine.py`、`app/application/request_scheduler.py`、`app/application/request_timing_service.py`、`web/static/*`。
- **本地验证**：
  - `python scripts/boundary_guard.py`：PASS
  - `python -m pytest tests/test_request_scheduling.py tests/test_boundary_guard_web_rules.py tests/test_boundary_guard_runtime_rules.py tests/test_boundary_guard_request_rules.py tests/test_boundary_guard_diagnostics_rules.py -q`：48 passed
  - `python -m pytest tests/test_live_freshness.py tests/test_live_overlay.py tests/test_floating_panel.py tests/test_reply_enqueue.py tests/test_web_routes.py -q`：102 passed
  - `python -m pytest tests/ -q`：992 passed, 5 skipped
  - `python main.py --web-browser`：启动后保持运行，34 秒超时前未见立即崩溃

---

## 最近变更（超 800 行治理 / TASK-main-pipeline-mic-runtime-split-1）

- **main.py + app/main_mic_mixin.py**：将麦克风链路与运行时辅助从 `main.py` 迁出到新的 `DanmuAppMicMixin`，包含 `_mic_audio_supported`、`_sync_mic_service`、`_calibrate_mic_noise_floor`、`_poll_mic_utterance`、`_on_mic_utterance_end`、`_has_mic_request_in_flight`、`_trigger_mic_api_call`、`_handle_mic_ai_reply`、`apply_danmu_read_config`、`run_danmu_read_probe`。
- **主链路边界保持不变**：`_trigger_api_call`、`_on_ai_reply`、`_consume_reply_queue` 仍留在 `main.py`；`QTimer`、`QThreadPool`、`QPixmap`、`reply_buffer`、`ai_in_flight`、`_pending_request_meta` 所有权未变。
- **行数复查**：`main.py` 由 `1834` 降至 `1636`；新增 `app/main_mic_mixin.py` 为 `234` 行；未引入新的超 800 行文件。
- **未触达关键区域**：`app/web_api/*`、`app/danmu_engine.py`、`app/overlay.py`、`app/application/request_scheduler.py`、`app/application/request_timing_service.py`。
- **本地验证**：
  - `python scripts/boundary_guard.py`：PASS
  - `python -m pytest tests/test_request_scheduling.py tests/test_boundary_guard_web_rules.py tests/test_boundary_guard_runtime_rules.py tests/test_boundary_guard_request_rules.py tests/test_boundary_guard_diagnostics_rules.py -q`：48 passed
  - `python -m pytest tests/ -q`：992 passed, 5 skipped
  - `python main.py --web-browser`：启动后保持运行，34 秒超时前未见立即崩溃

---

## 当前分支

**待项目负责人填写**（本地执行 `git branch --show-current`）。

## 当前阶段

W-019 已完成（P0 死代码与场景死状态清理）；W-PROMPT-001、W-018 等已完成。

## 已完成工单

| 工单 ID | 标题 | 完成日期 |
|---------|------|----------|
| W-000 | 建立 Codex 工作流文档与模板 | 2026-05-28 |
| W-001 | 清除 realtime/节奏模式主链路遗留死代码 | 2026-05-28 |
| W-002 | 死代码清理 + 截图退避最小接线 | 2026-05-28 |
| W-004 | 对齐小米 MiMo OpenAI 请求并修正默认视觉模型 | 2026-05-29 |
| W-006 | 已完成工单自动化回归验收与文档校正 | 2026-05-29 |
| W-PROVIDER-ADAPTER-001 | 引入 Provider 适配层收口多服务商差异 | 2026-05-29 |
| W-LIVE-OVERLAY-001 | OBS/直播伴侣网页弹幕层（SSE） | 2026-05-29 |
| W-005 | 补充小米 MiMo-V2.5 系列模型目录 | 2026-05-29 |
| W-ANNOUNCE-BADGE-001 | 修复公告已读后重启仍显示侧栏红点 | 2026-05-29 |
| W-008 | 温馨控制台顶部公告简略条 | 2026-05-29 |
| W-009 | pywebview 启动可靠性 | 2026-05-29 |
| W-010 | 托盘设置 open() 跨进程导航 | 2026-05-29 |
| W-011 | Web 启动失败可见提示 | 2026-05-29 |
| W-012 | 单实例锁与第二实例激活 | 2026-05-29 |
| W-013 | 简化模式下展示 API 温度调节 | 2026-05-29 |
| W-014 | pywebview 二阶段握手与 loaded 超时回退 | 2026-05-29 |
| W-015 | AiRunnable 对 `_request` 最终异常兜底，防止 ai_in_flight 卡死 | 2026-05-29 |
| W-016 | Web API 写操作经 WebConsoleBridge 主线程收口 | 2026-05-29 |
| W-017 | 修复热键修改后旧快捷键未注销 | 2026-05-29 |
| W-018 | SanitizedLogger 日志 UI 推送单例化 | 2026-05-29 |
| W-PROMPT-001 | 优化正式 AI 弹幕生成提示词体系 | 2026-05-29 |
| W-019 | P0 死代码与场景死状态清理 | 2026-05-29 |
| W-022 | 修复温馨控制台顶栏公告关闭后重启仍显示 | 2026-05-29 |
| W-ERROR-REPORT-001 | Supabase `error_reports` 表与客户端 API | 2026-05-29 |
| W-ERROR-REPORT-002 | Web `is_error` 错误反馈弹窗与日志提交 | 2026-05-29 |
| W-023 | 助手设置「恢复默认」 | 2026-05-29 |
| W-024 | 修复全量 pytest 8 项既有失败 | 2026-05-29 |
| W-APP-UPDATE-001 | 版本展示与更新提醒 | 2026-05-29 |
| W-STARTUP-001 | exe 冷启动分阶段耗时埋点 | 2026-05-30 |
| W-STARTUP-002 | 主线程短 wait + 非阻塞 pywebview | 2026-05-30 |
| W-MIMO-MIC-001 | 麦克风模式支持 MiMo（mimo-v2.5） | 2026-05-30 |
| W-DANMU-TTS-001 | 读弹幕后端（MiMo TTS + 播放） | 2026-05-30 |
| W-DANMU-TTS-002 | 读弹幕 Web 侧栏与专用 API | 2026-05-30 |
| W-DANMU-TTS-003 | 读弹幕验收热修（互斥播放/退出安全/句末尾韵） | 2026-05-30 |
| W-MODEL-CATALOG-PROBE-001 | 四平台 model_catalog 模型文本 + 识图 live 验收 | 2026-05-30 |
| W-025 | 新增测试1–4 内置人格并置顶人格工坊 | 2026-05-30 |
| W-E2E-001 | 全链路回归验收（浏览器控制台模式） | 2026-05-30 |
| W-AI-BUTLER-001 | Web「AI 管家」自然语言辅助助手设置 | 2026-05-31 |
| W-LAYOUT-OVERLAY-002 | layout_mode 缩小时 Overlay 重排与残影清理 | 2026-05-31 |
| W-AUDIT-001 | DanmuAI 全项目隐藏问题审计 | 2026-05-31 |
| W-AUDIT-FIX-001 | 修复 `/api/config` 保存假成功回执 | 2026-05-31 |
| W-AUDIT-FIX-002 | 修复审计剩余高优先级与测试基线问题 | 2026-05-31 |
| W-ERROR-REPORT-AUDIT-001 | Supabase `error_reports` 用户提交数据审计与问题登记 | 2026-06-01 |
| W-ERROR-REPORT-003 | 错误报告日志摘录（ISSUE-036） | 2026-06-01 |
| W-CONFIG-ENDPOINT-001 | 全局 api_endpoint 协议校验（ISSUE-039） | 2026-06-01 |
| W-ERROR-REPORT-004 | 诊断快照 config_context（ISSUE-037） | 2026-06-01 |
| W-ERROR-REPORT-005 | HTTP 长错误脱敏截断（ISSUE-038） | 2026-06-01 |
| W-ERROR-REPORT-006 | 错误反馈 localStorage 去重（ISSUE-040） | 2026-06-01 |
| W-NICKNAME-001 | 人格工坊新增昵称并接入 AI 弹幕生成提示词 | 2026-06-06 |
| W-LINT-001 | 一次性修复 9 处历史 ruff I001/F401（ISSUE-041） | 2026-06-06 |
| W-FP-001 | 新增 `display_mode` 与悬浮窗 6 项基础配置字段 | 2026-06-06 |
| W-FP-002 | 新建 `app/floating_panel.py` 悬浮窗组件本体 | 2026-06-06 |
| W-FP-003 | 悬浮窗接入 `_consume_reply_queue` 旁路分发 + Web 设置项 | 2026-06-06 |
| W-LIVE-TOPIC-001 | 人格工坊新增「提示内容」全局直播主题并注入 AI system 提示词 | 2026-06-06 |
| W-FONT-001 | 助手设置新增「字体设置」tab（横向弹幕 + 悬浮窗字体名/加粗/字号） | 2026-06-06 |
| W-FP-004 | 修复悬浮窗模式语义、独立出弹与停止清理（部分；实机由 W-FP-005 接续） | 2026-06-06 |
| W-FP-005 | 悬浮窗实机空白与 status 指标修复 | 2026-06-06 |
| W-COMMENT-P2-001 | `web/static/app.js` + `index.html` 补充注释 | 2026-06-06 |
| W-COMMENT-P2-002 | `web/static/modules/*.js` 7 文件补充注释 | 2026-06-06 |
| W-COMMENT-P2-003 | `warm-tokens.css` + `live-overlay.*` + `supabase-client.js` 补充注释 | 2026-06-06 |
| W-COMMENT-P2-004 | `scripts/boundary_guard*` 16 文件补充注释 | 2026-06-06 |
| W-COMMENT-P2-005 | `tests/conftest.py` + `fakes.py` + 配置 6 文件补充注释 | 2026-06-06 |

## 最近变更（W-FP-005）

- **app/floating_panel.py**：新条目落在窗口内底部可见区（修 enter_alpha=0 全空白）；`_kick_render` 不可见时 `singleShot` 重试；`DANMU_FLOATING_PANEL_DEBUG=1` 诊断日志；`feed` 不再按内部 overlay 模式拒收。
- **main.py**：`_sync_floating_panel_display_mode`；`_feed_floating_panel` 调试日志；`apply_config` / `set_display_mode` 拆分 try 块。
- **app/application/runtime_state.py** + **status_snapshot.py** + **web_console_support.py**：`display_mode`、`overlay_display_count`、`floating_panel_active_count`、`floating_panel_render_active`；`display_count` 按模式语义化。
- **web/static/modules/status.js**：在屏条数 tooltip 按 `display_mode` 区分。
- **tests/**：`test_floating_panel.py` 4 个 W-FP-005 用例 + `tests/test_status_floating_panel.py` 3 用例。
- 本地验证：`python -m pytest tests/ -q` **992 passed, 5 skipped**；`boundary_guard.py` **PASS**。

## 最近变更（W-COMMENT-P2-001~005）

合并执行 docs/work_orders/comment-current-files/03-p2-supporting-comments.md 5 个 P2 注释子工单，**仅**新增/扩充注释/docstring/区域分隔，不动任何逻辑：

- **P2-001** `web/static/app.js` + `web/static/index.html`：`app.js` 顶部 1 行 → 28 行 JSDoc（启动流程 6 阶段 / 模块分层 / 错误反馈链），`showToast` / `maybePromptErrorReport` / `init` 加 JSDoc；`index.html` 加 8 个 `<!-- ========== 区域 ========== -->` 块 + 7 个设置 tab 注释。
- **P2-002** `web/static/modules/*.js` 7 文件：每个模块顶部 1 行 → 8–12 行职责说明；`settings.js` 额外为 `collectFormData` / `fillForm` 加 JSDoc。
- **P2-003** `web/static/warm-tokens.css` 顶部 7 章节结构 + 不变量；`live-overlay.html` / `live-overlay.js` 顶部"OBS/直播伴侣透明页"职责说明；`supabase-client.js` 顶部 3 大表（announcements / feedback / error_reports）+ rate limit + IIFE 挂载。
- **P2-004** `scripts/boundary_guard.py` + `scripts/boundary_guard/` 全 16 Python 文件：每个模块顶部 docstring（职责 / 公共 API / 线程 / 维护者登记）。
- **P2-005** 根 `conftest.py`（早于 tests/conftest.py 执行 + TMP 重定向）+ `tests/conftest.py` 扩写 + `tests/fakes.py` 8 个 Fake 类（FakeLogger / FakeConfig / FakeLifetimeStats / FakeSessionRunLog / FakeEngine / FakeCapturer / FakeHistoryWriter / FakeTimer）class docstring + `pytest.ini` / `pyproject.toml` / `DanmuAI.spec` 顶部说明。
- 本地验证：`python scripts/boundary_guard.py` 5 次均 **PASS**；`python -m pytest tests/ -q` **992 passed, 5 skipped in 264.31s**（与 W-FP-005 基线一致）。P2-005 首次全量跑偶发 W-AUDIT-001 已知 flaky `test_trigger_api_call_increments_in_flight`，单跑与重跑全量均通过。
- 不修改 `app/`、`main.py`、`requirements.txt`、CI、锁文件；未新增/删除 import；未格式化；未提交 git（按用户"完成后输出完成报告"指示）。
- 报告：见 [templates/Codex完成报告/W-COMMENT-P2-001-005-完成报告.md](../templates/Codex完成报告/W-COMMENT-P2-001-005-完成报告.md)

## 最近变更（W-FP-004）

- **main.py**：新增 `_overlay_display_enabled` / `_sync_overlay_visibility`；`floating_panel` 模式不显示横向 Overlay；`_feed_floating_panel` 移出 `if item:`，不依赖 `add_text` 成功；`stop()` 调用 `panel.reset_session_state()`；`start()` 恢复悬浮窗显隐。
- **app/floating_panel.py**：新增 `reset_session_state()`（隐藏 + 清空运行态条目与去重窗口）。
- **tests/test_floating_panel.py**：追加 8 个 W-FP-004 回归用例（三模式 Overlay 显隐、拒收仍出弹、stop/start 生命周期、配置热切换）。
- **docs**：`ARCHITECTURE.md`、`WEB_CONSOLE.md` 更新 `display_mode` 语义与 stop 清理说明。

## 最近变更（W-FONT-001）

- **web/static/index.html**：助手设置新增第 7 个 tab「字体设置」（`data-settings-tab="font"`）；`font_size` 与 `floating_panel_font_size` 从「弹幕显示」tab 迁出；6 字段 + 共享 `<datalist>` 7 个常用字体。
- **web/static/modules/settings.js**：`CONFIG_FIELDS` 追加 2 个 family 键；`SETTINGS_RESTORE_GROUPS.font` / `SETTINGS_RESTORE_CHECKBOXES.font`；`collectFormData` / `fillForm` / `applyDefaultToField` 扩展 bold checkbox；5 条 `SETTINGS_FIELD_TIPS`。
- **app/config_defaults.py**：追加 `danmu_font_family` / `danmu_font_bold` / `floating_panel_font_family` / `floating_panel_font_bold` 默认值。
- **app/application/config_service.py**：`WEB_CONFIG_KEYS` 追加 4 键；`_normalize_items` 追加 `font_size` 12–72 钳位、bold 0/1 归一、family 空串兜底。
- **app/overlay.py**：`_apply_font_from_config` 读 family/bold；`display_settings_dirty` 跟踪 family/bold marker。
- **app/floating_panel.py**：`_apply_config` 读 family/bold；新增 `_rebuild_active_pixmaps` 在 `apply_config` 末尾重建在飞 item pixmap。
- **tests/**：新增 9 用例（`test_config_defaults` 4 + `test_web_routes` 3 + `test_floating_panel` 2 + `test_overlay_render` 1）。
- 本地验证：`python -m pytest tests/ -q` **958 passed, 5 skipped**；`python scripts/boundary_guard.py` **PASS**。
- **未触达**：`main.py` / `app/danmu_engine.py` / `app/ai_client.py` / `app/mic_*.py` / `app/web_console.py` / `scripts/boundary_guard*`。
- 完成报告：[工单/W-FONT-001-完成报告.md](工单/W-FONT-001-完成报告.md)

## 最近变更（W-LIVE-TOPIC-001）

- 人格工坊顶部（昵称上方）新增「提示内容」textarea + 「保存主题」按钮；走现有 `PUT /api/config`，新键 `live_topic` 持久化到 `config.db`（上限 200 字）。
- 新增助手函数 `app.persona_contract.append_live_topic_to_system_pt`：空值/键缺失/纯空白时原样返回 `system_pt`；非空时按 `Translator.get_language()` 在末尾追加 `[本次直播主题：xxx；…]` / `[Live stream topic: xxx; …]`。
- `main.py` 两处 `system_pt` 取出点（视觉 API / 麦克风插入）各在 `append_nickname_to_system_pt` 之后加一行 `append_live_topic_to_system_pt`。
- `WEB_CONFIG_KEYS` / `CONFIG_DEFAULTS` 追加 `live_topic`；「恢复默认」会清空 `live_topic`（与 `user_nickname` 一致）。
- 完成报告：[W-LIVE-TOPIC-001-完成报告](../templates/Codex完成报告/W-LIVE-TOPIC-001-完成报告.md)

## 最近变更（W-FP-003）

- **main.py**：
  - `DanmuApp._consume_reply_queue` 末尾追加 1 行 `self._feed_floating_panel(display_content, queued.persona_id)`（**不**改既有 `if item:` 块语义）。
  - 新增 2 个私有方法 `_feed_floating_panel` / `_floating_panel_enabled`；异常吞掉 + debug 日志（绝不阻断主链路）。
  - `_on_config_changed` 末尾追加 4 行 `panel.apply_config()` + `panel.set_display_mode(...)`（主线程 signal 链路）。
- **web/static/index.html**：「弹幕显示」tab 末尾追加 6 个表单字段（`<select>` × 1 + `<input type="number">` × 4 + `<input type="checkbox">` × 1）。
- **web/static/modules/settings.js**：`CONFIG_FIELDS` / `SETTINGS_RESTORE_GROUPS.danmu` / `applyDefaultToField` / `fillForm` / `collectFormData` 各追加 6 键处理；`display_mode` enum 校验；6 条 help 文案。
- **docs/runtime-state-map.md**：顶部说明加入 `floating_panel`，与 `overlay` 并列。
- **scripts/boundary_guard/constants.py**：`RUNTIME_FIELD_EXCLUDE` 加入 `floating_panel`（与 `overlay` 同处理）。
- **tests/test_floating_panel.py** 新增 5 个 W-FP-003 端到端用例（`_floating_panel_enabled` 判定、feed routing、overlay-mode skip、异常吞掉、`_consume_reply_queue` spy）。
- **tests/test_web_routes.py** 新增 5 个 PUT /api/config 用例（display_mode 持久化 + reload、非法回落、6 键 round-trip、speed 钳位、click_through 归一）。
- 本地验证：`python -m pytest tests/ -q` **937 passed, 5 skipped**；`python scripts/boundary_guard.py` **PASS**。
- **未触达**：`app/danmu_engine.py` / `app/overlay.py` / `app/ai_client.py` / `app/danmu_pool.py` / `app/floating_panel.py` / `requirements.txt`。
- 工单文件：[工单/W-FP-003.md](工单/W-FP-003.md)

## 最近变更（W-FP-002）

- **新增** `app/floating_panel.py::FloatingPanel(QWidget)`：独立弹幕姬式悬浮窗；事件驱动渲染（队列空 → `QTimer.stop()`）；Win32 透明置顶（`WS_EX_LAYERED | WS_EX_TRANSPARENT` 可切换）；6 项配置 `apply_config()` 热更新；客户区拖动 + 右下角 `QSizeGrip` 缩放；底 30px 渐入 / 顶 30px 渐隐。
- **main.py**：`DanmuApp.__init__` 末尾追加 4 行（导入 + 实例化 + `set_display_mode` 读 ConfigStore）；不接入 `_consume_reply_queue`（由 W-FP-003 处理）。
- **新增** `tests/test_floating_panel.py`：**13 个用例** — 初始 timer 非 active、feed 启动、滚出顶部自动停表、max_items FIFO 裁剪、apply_config 反映改动、display_mode 显隐/非法回落、deque 去重、空文本忽略、overlay 模式拒绝 feed、钳位（font_size=999→48 / opacity=200→100 / max_items=0→5 / speed=100→5.0）。
- 本地验证：`python -m pytest tests/test_floating_panel.py -q` **13 passed**；`python -m pytest tests/test_config_defaults.py -q` **9 passed**。
- **未触达**：`app/danmu_engine.py`、`app/overlay.py`、`app/web_api/routes.py`、`web/static/`、`requirements.txt`、主链路 `_consume_reply_queue`（由 W-FP-003 接管）。
- 工单文件：[工单/W-FP-002.md](工单/W-FP-002.md)

## 最近变更（W-FP-001）

- `app/config_defaults.py::CONFIG_DEFAULTS` 追加 6 键：`display_mode="overlay"`、`floating_panel_opacity="85"`、`floating_panel_font_size="18"`、`floating_panel_max_items="60"`、`floating_panel_speed="1.5"`、`floating_panel_click_through="1"`。
- `app/application/config_service.py`：`WEB_CONFIG_KEYS` 元组追加 6 键；`_normalize_items` 新增 `display_mode` 枚举校验（非法 → `overlay`）、4 个 int 字段用 `_clamp_int_key` 钳位、`floating_panel_speed` float 钳位（0.5-5.0，3 位小数）、`floating_panel_click_through` 布尔归一。
- `tests/test_config_defaults.py` 新增 8 个用例：默认值存在、`WEB_CONFIG_KEYS` 包含、seed 落库、`display_mode` 非法回落与大小写归一、3 个 int 字段边界（200/12/0）。
- 本地验证：`python -m pytest tests/test_config_defaults.py -q` **9 passed**。
- **未触达**：`app/danmu_engine.py`、`app/overlay.py`、`main.py`、`app/web_api/routes.py`、UI 字段（由 W-FP-003 接入）。
- 工单文件：[工单/W-FP-001.md](工单/W-FP-001.md)；完成报告（与 W-FP-002 合并）：[W-FP-001-002-完成报告](../templates/Codex完成报告/W-FP-001-002-完成报告.md)（W-FP-002 收尾后补全）

## 最近变更（W-LINT-001）

- `python -m ruff check --fix` 于 7 个已跟踪文件：删除 3 处死 import（`app/live_freshness.py` 的 `time`、`app/region_selector.py` 函数内 `resolve_screen_index`、`tests/test_ai_pipeline.py` 的 `FakeConfig`、`tests/test_danmu_display_cap.py` 的 `pytest`）；整理 5 处 `I001` import 排序。
- 删除无引用临时脚本 `scripts/rebalance_t008_tests2.py`（W-008 期间一次性 rebalance 脚本，方案 A）。
- 本地验证：
  - `python -m ruff check app main.py tests scripts`：**All checks passed!**
  - `python -m pytest tests/ -q`：**906 passed, 5 skipped**
  - `python scripts/boundary_guard.py`：**PASS**
- ISSUE-041 标为**已修复**；ruff **未**接入 CI（后续 `W-CI-LINT-001`）。
- 完成报告：[W-LINT-001-完成报告](../templates/Codex完成报告/W-LINT-001-完成报告.md)

## 最近变更（W-ERROR-REPORT-004/005/006）

- **W-ERROR-REPORT-004**：`build_diagnostic_snapshot()` 增加 `config_context`；Web `buildDiagnosticReportText` 同步。
- **W-ERROR-REPORT-005**：`sanitize_provider_error_snippet` + 长 HTTP message 不再一律「详情已隐藏」。
- **W-ERROR-REPORT-006**：`error_report` 去重 map 使用 `localStorage`（自 session 迁移）。
- `python -m pytest tests/test_diagnostics.py tests/test_ai_client.py tests/test_bundle_paths.py -q` — **55 passed**。

## 最近变更（W-LINT-001 登记）
- 从 W-NICKNAME-001 完成报告 §8 派生：把"9 处历史 ruff I001/F401（ISSUE-041）"沉淀为新工单 `W-LINT-001`（待办）。
- 同步登记 `W-CI-LINT-001`（待办；CI 加 `ruff check` 防退步），须 W-LINT-001 收尾后立项。
- 工单正文：[docs/工单列表/工单/W-LINT-001.md](工单/工单/W-LINT-001.md)；状态登记：[docs/工单列表.md](工单列表.md) 顶部两行；ISSUE-041 链接：[docs/已知问题与后续事项.md](已知问题与后续事项.md)。
- 本节**未**执行任何代码修改（W-LINT-001 由后续负责人/Codex 开工）。

## 最近变更（W-NICKNAME-001）
- 人格工坊顶部新增「昵称」输入框 + 「保存昵称」按钮；走现有 `PUT /api/config`，新键 `user_nickname` 持久化到 `config.db`。
- 新增助手函数 `app.persona_contract.append_nickname_to_system_pt`：`config` 空值/键缺失/纯空白时原样返回 `system_pt`；非空时按 `Translator.get_language()` 在末尾追加 `[用户昵称：xxx；…]` / `[User nickname: xxx; …]`；长度上限 20 字符，前后端双限。
- `main.py` 两处 `system_pt` 取出点（视觉 API 调用点 / 麦克风插入点）各加一行 `append_nickname_to_system_pt(system_pt, self.config)`；主链路时序、`ai_in_flight` 闸门、`RequestScheduler` / `RequestTimingService` 未改。
- `WEB_CONFIG_KEYS` / `CONFIG_DEFAULTS` 同步追加 `user_nickname`（默认空串）；`GET /api/config` 自动带出（走现有 `export_config` 全量导出）。
- `tests/test_reply_contract.py` 新增 8 项 nickname 注入单测；`tests/test_web_routes.py` 新增 3 项 `ConfigService` 端到端（持久化往返 / 键白名单 / 默认值）。
- 本地验证：
  - `python -m pytest tests/test_reply_contract.py tests/test_web_routes.py -q`：**36 passed**
  - `python -m pytest tests/ -q`：**906 passed, 5 skipped**
  - `python scripts/boundary_guard.py`：**PASS**
  - `python -m ruff check app/persona_contract.py app/personae.py app/config_defaults.py app/application/config_service.py main.py tests/test_reply_contract.py tests/test_web_routes.py`：**All checks passed!**
- 完成报告：[W-NICKNAME-001-完成报告](../templates/Codex完成报告/W-NICKNAME-001-完成报告.md)；登记 ISSUE-041（历史 import-order 警告）。

## 最近变更（W-ERROR-REPORT-003 / W-CONFIG-ENDPOINT-001）

- **W-ERROR-REPORT-003**：`extractErrorReportSearchTerms` / `findErrorLogAnchorIndex`；退避文案解析「最近错误」与 HTTP 码；时间窗内合并全部 ERROR/WARNING。
- **W-CONFIG-ENDPOINT-001**：`validate_web_config_patch` 对非空 `api_endpoint` 调用 `is_valid_endpoint`；i18n `config.error_api_endpoint_invalid`。
- `python -m pytest tests/test_bundle_paths.py tests/test_model_selection.py tests/test_supabase_static.py -q` — **26 passed**。
- 完成报告：[W-ERROR-REPORT-003](../templates/Codex完成报告/W-ERROR-REPORT-003-完成报告.md)、[W-CONFIG-ENDPOINT-001](../templates/Codex完成报告/W-CONFIG-ENDPOINT-001-完成报告.md)。

## 最近变更（W-ERROR-REPORT-AUDIT-001）

- Supabase MCP 只读审计 `public.error_reports`：**9** 条（2026-05-31，`app_version=0.2.2`）；7 个 `client_id`、7 个 `error_fingerprint`。
- 上报链路可用，但运维可排障性不足：退避类 summary 的 `logs_excerpt` 常无 ERROR/无 HTTP 码字面量；3 条 summary 为「HTTP … (详情已隐藏)」；诊断 JSON 缺 `active_model_id`/provider。
- 用户根因聚类：无协议 `api_endpoint`（3 条）、模型不存在（1）、429（1）、HTTP 400/500/405（4，405 未在本工单查上游）。
- 登记 ISSUE-036～040；未改 `app/`、`web/`、`main.py`。
- 完成报告：[templates/Codex完成报告/W-ERROR-REPORT-AUDIT-001-完成报告.md](../templates/Codex完成报告/W-ERROR-REPORT-AUDIT-001-完成报告.md)

## 最近变更（W-AUDIT-FIX-002）
- `SingleInstanceGuard` 不再把 `listen()` 失败当作获取主实例成功；只在确认可回收陈旧 server name 后才重试监听，避免双开误判和误删有效实例端点。
- `DanmuApp.quit()` 改为先等待 `QThreadPool.globalInstance().waitForDone(2000)`，再关闭 `ai_worker` 的 HTTP client；超时会写 warning，避免 worker 访问已关闭 client。
- `app/startup_trace.py` 将 Web 控制台同步等待窗口收紧到开发环境 `0.5s`、打包环境 `1.5s`，降低双击 exe 时长时间空窗。
- `tests/test_single_instance.py` 在 Windows + Python 3.14 下提前 module skip，消除了 `PyQt6.QtNetwork` 导入后置崩溃；README 同步改为“Python 3.12 推荐”。
- `HistoryWriter.flush()` 不再静默吞掉 SQLite 异常，现会写异常日志；新增 `tests/test_history_writer.py`。
- `web/static/app.js` 为降级状态轮询错误 toast 增加 30 秒节流，避免每 1.5 秒重复弹错。
- `app/mic_test_send.py` 改为通过 `DanmuApp` 公共 facade 访问麦克风能力、采样和 probe 入口，减少对 `_mic_service` / `_mic_audio_supported` 的直接耦合。
- 本地验证：
  - `python -m pytest tests/ -q`：**750 passed, 1 skipped**
  - `python -m ruff check app main.py tests scripts`：**PASS**
  - `python scripts/boundary_guard.py`：**PASS**
- 完成报告：[templates/Codex完成报告/W-AUDIT-FIX-002-完成报告.md](../templates/Codex完成报告/W-AUDIT-FIX-002-完成报告.md)

## 最近变更（W-AUDIT-FIX-001）
- `/api/config` 改为等待主线程保存显式回执：只有 `apply_web_config_payload()` 成功完成才返回 `{"ok": true}`。
- 新增 `save_config_via_bridge()` 保存回执辅助函数；保存超时返回 `504` + `{"ok": false, "error": "save_timeout", "detail": ...}`，主线程保存异常返回 `500` + `{"ok": false, "error": "save_failed", "detail": ...}`。
- `WebConsoleBridge._on_save_config()` 现在会先写回结果，再释放等待事件，避免 HTTP 线程读到旧的默认超时结果。
- 回归测试新增 3 项：保存成功、主线程超时、主线程异常。
- 本工单不处理“OBS / 直播伴侣采集不到”问题；用户已确认浏览器源场景可见。
- 本地验证：
  - `python -m pytest tests/test_web_console.py -q`：**63 passed**
  - `python scripts/boundary_guard.py`：**PASS**
  - `python -m ruff check app/web_console.py tests/test_web_console.py`：**PASS**
- 完成报告：[templates/Codex完成报告/W-AUDIT-FIX-001-完成报告.md](../templates/Codex完成报告/W-AUDIT-FIX-001-完成报告.md)

## 最近变更（W-AUDIT-001）

- 新增审计报告：[audits/full-project-hidden-issues-audit.md](audits/full-project-hidden-issues-audit.md)，覆盖启动、Web/API、主链路、配置持久化、并发、打包、CI 和日志可诊断性。
- 本地验证结果：
  - `python scripts/boundary_guard.py`：**PASS**
  - `python -m ruff check app main.py tests scripts`：**FAIL**，命中 `tests/test_webview_shell.py:99` 的 `I001`
  - `python -m pytest tests/ -q`：**745 passed, 1 skipped**，但进程退出后仍打印 `Windows fatal exception: code 0xc0000139`
  - `python -m pytest tests/test_boundary_guard.py tests/test_request_scheduling.py -q`：**41 passed**
  - Overlay / AI provider / SQLite 子集测试均通过
- 高优先级发现摘要：
  - `/api/config` 保存超时仍返回成功
  - `SingleInstanceGuard` 在 `listen()` 失败和 `removeServer()` 路径上存在误判/误删风险
  - 启动阶段主线程同步等待 Web 控制台就绪，可能造成双击 exe 空窗
  - `quit()` 中先关闭 `httpx` 客户端再等待线程池，存在退出竞态
  - pytest 当前存在“退出码 0 但进程后置原生崩溃”的隐藏风险
- 完成报告：[templates/Codex完成报告/W-AUDIT-001-完成报告.md](../templates/Codex完成报告/W-AUDIT-001-完成报告.md)

## 最近变更（W-LAYOUT-OVERLAY-002）

- `layout_mode` 从 fullscreen 缩小到 1/2、1/4、3/4 时：`reload_tracks(clip_to_drawable=True)` 丢弃新可绘制带外的在屏弹幕，避免 `_nearest_track_for_y` 把下半屏条目挤到底部轨道。
- `DanmuOverlay.show_for_screen` 在 ratio 缩小时对旧可绘制高度整带 `update`，下一帧 `paintEvent` 对 clip 执行 `CompositionMode_Clear`，清除下半屏残影。
- 单测 [`tests/test_layout_mode_overlay.py`](../tests/test_layout_mode_overlay.py) 5 项；相关回归 20 passed；`boundary_guard` PASS。
- 完成报告：[templates/Codex完成报告/W-LAYOUT-OVERLAY-002-完成报告.md](../templates/Codex完成报告/W-LAYOUT-OVERLAY-002-完成报告.md)

## 最近变更（W-AI-BUTLER-001）

- 侧栏新增 **AI 管家**：`POST /api/ai-butler/chat` 复用助手设置 API 做纯文本对话；返回可审阅 `patch`，用户确认后 `POST /api/config` 写入。
- 后端 [`app/web_api/ai_butler.py`](../app/web_api/ai_butler.py)：17 字段白名单、JSON 解析兜底、不经 `AiWorker` / `ai_in_flight`。
- 单测 [`tests/test_ai_butler.py`](../tests/test_ai_butler.py) 8 项；全量 pytest **735 passed**；`boundary_guard` PASS。

## 目录模型 live 验收（W-MODEL-CATALOG-PROBE-001）

| 指标 | 结果 |
|------|------|
| 模型数 | 24（catalog 当前全量） |
| 文本 ping | 21/24 |
| 最小识图 | 23/24 |
| 失败（已处理） | ISSUE-027/028 三模型已从 `model_catalog` 移除（2026-05-30） |
| 产物 | `.pytest_tmp/probe_results.json`（本地，不提交） |

完成报告：[templates/Codex完成报告/W-MODEL-CATALOG-PROBE-001-完成报告.md](../templates/Codex完成报告/W-MODEL-CATALOG-PROBE-001-完成报告.md)

## 当前目录结构摘要

```text
danmu/
├─ main.py              # DanmuApp 引导
├─ app/                 # 核心业务（AI、Overlay、Web API、配置）
├─ web/static/          # Web 控制台 UI
├─ tests/               # pytest
├─ docs/                # 文档（含 workflow、templates、Codex 工作流）
├─ scripts/             # boundary_guard 等
└─ data/                # 弹幕库等资源
```

详见 [ai-project-context.md](ai-project-context.md) §4。

## 已安装依赖

- **Python**：≥ 3.12（CI：`windows-latest` + 3.12）
- **安装**：`pip install -r requirements.txt`

## 可用脚本

| 命令 | 用途 |
|------|------|
| `python main.py` | Web + pywebview + Overlay |
| `python main.py --web-browser` | 系统浏览器打开控制台 |
| `python -m pytest tests/ -q` | 全量测试 |
| `python scripts/boundary_guard.py` | 架构回归扫描（变更编排/Web 时） |

## 构建状态

未在本次文档工单中执行打包。发布见 [PACKAGING_WINDOWS.md](PACKAGING_WINDOWS.md)。

## 测试状态

**W-AI-BUTLER-001（当前）**：`python -m pytest tests/ -q` — **735 passed, 1 skipped**；`python scripts/boundary_guard.py` — **PASS**（2026-05-31）。完成报告：[W-AI-BUTLER-001-完成报告](../templates/Codex完成报告/W-AI-BUTLER-001-完成报告.md)。

**W-E2E-001**：`python -m pytest tests/ -q` — **727 passed, 1 skipped**；`python scripts/boundary_guard.py` — **PASS**；RELEASE 子集 84+59+116 passed（2026-05-30）。完成报告：[W-E2E-001-完成报告](../templates/Codex完成报告/W-E2E-001-完成报告.md)。

**W-025**：`python -m pytest tests/test_reply_contract.py tests/test_web_persona_api.py -q` — **26 passed**；全量 **727 passed, 1 skipped**（2026-05-30）。

**W-MIMO-MIC-001**：`python -m pytest tests/ -q` — **711 passed, 1 skipped**（2026-05-30）。MiMo `mimo-v2.5` 开麦走 Chat Completions `input_audio`；真实 Key 手动验收见 [W-MIMO-MIC-001-完成报告](../templates/Codex完成报告/W-MIMO-MIC-001-完成报告.md) §6。

**W-APP-UPDATE-001**：`python -m pytest tests/ -q` — **700 passed, 1 skipped**；`python scripts/boundary_guard.py` — **PASS**（2026-05-29）。新增 `tests/test_version_compare.py`、`tests/test_version_api.py`。

**W-024**：`python -m pytest tests/ -q` — **690 passed, 1 skipped**（2026-05-29）。修复 `test_danmu_pool_routes_registered` 与 7 项 `LogEmitBus` 全量失败。

**W-023**：`python -m pytest tests/test_web_console.py::test_export_web_config_defaults tests/test_web_console.py -q` — **60 passed**；`python scripts/boundary_guard.py` — **PASS**（2026-05-29）。

**W-PROMPT-001（当前）**：`python -m pytest tests/test_reply_contract.py tests/test_reply_parser.py tests/test_web_persona_api.py -q` — **36 passed**（2026-05-29）。

**W-018**：`python -m pytest tests/test_p1_log_sanitization.py -q` — **12 passed**；`python -m pytest tests/test_p0_main_flow.py -q` — **29 passed**（2026-05-29）。

**W-017**：`python -m pytest tests/test_hotkey.py -q` — **4 passed**；`python -m pytest tests/ -q` — **679 passed, 1 failed, 1 skipped**（失败项 `test_danmu_pool_routes_registered` 与 W-017 无关，2026-05-29）。

**W-016**：`python -m pytest tests/test_web_console.py tests/test_web_persona_api.py tests/test_web_custom_models.py -q` — **69 passed**；`python scripts/boundary_guard.py` — **PASS**（2026-05-29）。

**W-015**：`python -m pytest tests/ -q` — **674 passed, 1 skipped**；`tests/test_p0_main_flow.py` — **29 passed**（2026-05-29）。

**W-014**：`python -m pytest tests/ -q` — **672 passed, 1 skipped**；`tests/test_webview_shell.py` — **13 passed**（2026-05-29）。

**W-009～W-012**：`python scripts/boundary_guard.py` — **PASS**（2026-05-29）。

**W-008**：`python -m pytest tests/test_bundle_paths.py tests/test_supabase_static.py -q` — **9 passed**（2026-05-29）。

**W-022**：`python -m pytest tests/test_web_console.py -q -k announcements_read_state` — **3 passed**；`python scripts/boundary_guard.py` — **PASS**（2026-05-29）。

**W-ERROR-REPORT-001/002**：`python -m pytest tests/test_supabase_static.py tests/test_bundle_paths.py -q` — **13 passed**（2026-05-29）；Supabase MCP `apply_migration`（`error_reports`）— success。

**W-ANNOUNCE-BADGE-001**：`python -m pytest tests/test_web_console.py -q -k announcements_read_state` — **3 passed**（2026-05-29）。

**W-005**：`python -m pytest tests/test_model_catalog.py tests/test_web_console.py::test_model_catalog_api_payload -q` — **19 passed**（2026-05-29）。

**W-LIVE-OVERLAY-001**：`python -m pytest tests/ -q` — **658 passed**（2026-05-29；含 `tests/test_live_overlay.py` 7 项）。

**W-PROVIDER-ADAPTER-001**：`python -m pytest tests/ -q` — **651 passed**（2026-05-29；含 `tests/test_provider_adapters.py`）。`python scripts/boundary_guard.py` — PASS。

**W-006**：641 passed（历史）。

历史基线：W-002 收尾 638 passed（移除 6 个库存预取专测）；W-004 交付时 644 passed（W-002 删减前）。

## 手动验收状态

**W-E2E-001**：自动化 + API smoke **通过**；B2 视觉主链路 / B4 开麦 / B5 读弹幕 TTS / B3.5 部分 Web UI 项 **待负责人** 用 API Key 补签（见 [W-E2E-001-手动验收](templates/手动验收/W-E2E-001-手动验收.md)）。

W-004（MiMo）：自动化单测通过（W-006 复验 641 passed）；真实小米 Key 下弹幕入屏见 [W-004-完成报告](../templates/Codex完成报告/W-004-完成报告.md) §6（待负责人签字）。须 Web 选小米 MiMo、`mimo-v2.5` 并**保存配置**。

W-000（文档工单）：通过标准见工单完成报告 — 确认 `docs/` 与 `AGENTS.md` 链接完整、无业务代码改动。

## 已知问题

摘要见 [已知问题与后续事项.md](已知问题与后续事项.md)。

## 最近一次修改摘要

**W-ERROR-REPORT-006（2026-06-01）**：错误反馈指纹去重迁至 `localStorage`（ISSUE-040）。完成报告：[W-ERROR-REPORT-006-完成报告](../templates/Codex完成报告/W-ERROR-REPORT-006-完成报告.md)。

**W-ERROR-REPORT-005（2026-06-01）**：长 HTTP 错误 body 脱敏截断后写入用户可见摘要（ISSUE-038）。完成报告：[W-ERROR-REPORT-005-完成报告](../templates/Codex完成报告/W-ERROR-REPORT-005-完成报告.md)。

**W-ERROR-REPORT-004（2026-06-01）**：诊断快照 `config_context`（ISSUE-037）。完成报告：[W-ERROR-REPORT-004-完成报告](../templates/Codex完成报告/W-ERROR-REPORT-004-完成报告.md)。

**W-CONFIG-ENDPOINT-001（2026-06-01）**：`PUT /api/config` 拒绝无 http(s) 的 `api_endpoint`（ISSUE-039）。完成报告：[W-CONFIG-ENDPOINT-001-完成报告](../templates/Codex完成报告/W-CONFIG-ENDPOINT-001-完成报告.md)。

**W-ERROR-REPORT-003（2026-06-01）**：改进错误报告 `logs_excerpt` 摘录（ISSUE-036）。完成报告：[W-ERROR-REPORT-003-完成报告](../templates/Codex完成报告/W-ERROR-REPORT-003-完成报告.md)。

**W-ERROR-REPORT-AUDIT-001（2026-06-01）**：只读审计 Supabase `error_reports`（9 条）；文档登记 ISSUE-036～040。完成报告：[W-ERROR-REPORT-AUDIT-001-完成报告](../templates/Codex完成报告/W-ERROR-REPORT-AUDIT-001-完成报告.md)。

**移除固定测试弹幕种子（2026-05-30）**：已删除 `danmu_demo_seed_*` 数据/模块与 `main.py` 定时入轨逻辑；恢复仅公式化池 + AI 上屏。

**W-E2E-001（2026-05-30）**：全链路回归验收（`--web-browser`）。完成报告：[W-E2E-001-完成报告](../templates/Codex完成报告/W-E2E-001-完成报告.md)。

**W-025（2026-05-30）**：新增内置人格测试1–4（五人弹幕试验提示词，测试2 竞技/测试3 唠嗑/测试4 阴阳梗）；`BUILTIN_PERSONA_PINNED_FIRST` 置顶人格工坊列表；移除旧「测试」并加入 `_REMOVED_PERSONAE`。完成报告：[W-025-完成报告](../templates/Codex完成报告/W-025-完成报告.md)。

**W-APP-UPDATE-001（2026-05-29）**：侧栏左下角版本展示；`app/version.py` + `GET /api/version`；Supabase `app_updates` + 更新弹窗；`app_update_state` 忽略持久化；数字段版本比较。完成报告：[W-APP-UPDATE-001-完成报告](../templates/Codex完成报告/W-APP-UPDATE-001-完成报告.md)。

**W-024（2026-05-29）**：修复全量 pytest 8 项既有失败——`test_danmu_pool_routes_registered` stub `invoke_on_main`；`tests/conftest.py` autouse 重置 `_log_bus`；`get_log_bus()` 经 `sip.isdeleted` 防御重建。全量 **690 passed, 1 skipped**。完成报告：[W-024-完成报告](../templates/Codex完成报告/W-024-完成报告.md)。

**W-023（2026-05-29）**：助手设置新增「恢复默认」次要按钮与确认弹窗（仅当前 Tab / 全部）；`GET /api/config/defaults` 为唯一默认值来源；恢复只改表单不自动保存，API Key 不参与。完成报告：[W-023-完成报告](../templates/Codex完成报告/W-023-完成报告.md)。

**W-ERROR-REPORT-002（2026-05-29）**：`is_error` 时 Web 模态框确认后提交 `error_reports`（日志 ±90s/±40 行 + `/api/diagnostics`）；24h fingerprint 去重；未配置 Supabase 静默。完成报告：[W-ERROR-REPORT-002-完成报告](../templates/Codex完成报告/W-ERROR-REPORT-002-完成报告.md)。

**W-ERROR-REPORT-001（2026-05-29）**：`002_error_reports.sql` + `DanmuSupabase.submitErrorReport`。完成报告：[W-ERROR-REPORT-001-完成报告](../templates/Codex完成报告/W-ERROR-REPORT-001-完成报告.md)。

**W-022（2026-05-29）**：`announcements_read_state` 增加 `overviewBannerDismissedId`；顶栏公告关闭写入 `config.db`（与 localStorage 双写）；修复 ISSUE-016。完成报告：[W-022-完成报告](../templates/Codex完成报告/W-022-完成报告.md)。

**W-018（2026-05-29）**：`LogEmitBus` / `get_log_bus()` 单例；`SanitizedLogger._emit` 统一经全局 bus 推送 Web 日志；临时实例（AiRunnable、HotkeyManager 等）日志现可进入控制台日志环。完成报告：[W-018-完成报告](../templates/Codex完成报告/W-018-完成报告.md)。

**W-017（2026-05-29）**：`HotkeyManager` 引入 `_registered_hotkey_str`，`unregister()` 按实际已注册键移除；修复 Web 改热键后旧键仍有效（ISSUE-013）。完成报告：[W-017-完成报告](../templates/Codex完成报告/W-017-完成报告.md)。

**W-016（2026-05-29）**：`WebConsoleBridge.invoke_on_main`（`BlockingQueuedConnection`）；`routes.py` 全部写路由经 `_invoke_main` 在主线程执行（公告已读、活跃人格、麦克风测试、人格/弹幕库/自定义模型）；只读 GET 与 probe 保持 HTTP 线程。完成报告：[W-016-完成报告](../templates/Codex完成报告/W-016-完成报告.md)。

**W-015（2026-05-29）**：`AiRunnable.run()` 对 `worker._request()` 增加最终异常兜底，经 `error` 信号释放 `ai_in_flight`/`mic_in_flight`；修复 ISSUE-012。完成报告：[W-015-完成报告](../templates/Codex完成报告/W-015-完成报告.md)。

**W-008（2026-05-29）**：温馨控制台顶栏展示最新公告「标题：正文」前 30 字；`danmu_announcements_overview_banner_dismissed_id` 记录关闭的公告 id；与侧栏红点独立。完成报告：[W-008-完成报告](../templates/Codex完成报告/W-008-完成报告.md)。

**W-ANNOUNCE-BADGE-001（2026-05-29）**：公告未读改为按 Supabase 公告 `id` 判定；`GET/PUT /api/announcements-read-state` 写入本机 `config.db`；`app.js` 启动时先加载已读再拉 Supabase，进入公告页即隐藏红点；迁移旧 `danmu_announcements_last_seen_at` 时间戳键。

**W-005（2026-05-29）**：`MIMO_MODELS` 对齐 MiMo-V2.5 显示名/定价（¥1/¥2），新增 `mimo-v2.5-pro`（¥3/¥6）；默认仍为 `mimo-v2.5`。完成报告：[W-005-完成报告](../templates/Codex完成报告/W-005-完成报告.md)。

**W-LIVE-OVERLAY-001（2026-05-29）**：新增 `LiveOverlayHub`、`/live-overlay` 透明页、`/api/live-overlay/events` SSE、`POST …/test`；`main.py` 在 normalize 成功后旁路广播（视觉 AI + 开麦 AI）；控制台「直播输出」区。未改 Qt Overlay 与入队/上屏主链路。完成报告：[W-LIVE-OVERLAY-001-完成报告](../templates/Codex完成报告/W-LIVE-OVERLAY-001-完成报告.md)。

**W-PROVIDER-ADAPTER-001（2026-05-29）**：新增 `app/providers/`（registry、capabilities、Default/MiMo adapters）；合并 endpoint guess 与 host marker 为 `HOST_ENTRIES`；`ai_client` / `api_probe` OpenAI 路径委托 adapter；未改 `main.py` 主链路。完成报告：[W-PROVIDER-ADAPTER-001-完成报告](../templates/Codex完成报告/W-PROVIDER-ADAPTER-001-完成报告.md)。

**W-021（2026-05-29）**：`DefaultOpenAIAdapter` 仅在 `stream: true` 时注入 `stream_options`；修复百炼/DashScope「测试连接」HTTP 400（ISSUE-015）。主链路流式弹幕请求行为不变。完成报告：[W-021-完成报告](../templates/Codex完成报告/W-021-完成报告.md)。

**W-019（2026-05-29）**：删除 `app/history.py`（`DanmuHistory`）、未用 `app/` 符号与 `main` 场景 hash/gate 死状态；历史上屏仍走 `HistoryWriter`。场景探测未恢复（ISSUE-014 → W-020）。完成报告：[W-019-完成报告](../templates/Codex完成报告/W-019-完成报告.md)。

**W-006（2026-05-29）**：对 W-001/W-002/W-004 与 WebSocket 调试交付做自动化回归；新增 `tests/test_web_console.py` 3 个 WS 回归测试；校正 W-001/W-004 完成报告测试计数。ISSUE-003~007 仍为待办。完成报告：[W-006-完成报告](../templates/Codex完成报告/W-006-完成报告.md)。

**W-002 收尾（2026-05-29）**：确认 main 无库存预取死链、`probe_runnable` 已删、退避已接 `screenshot_interval_ms`；精简 `test_danmu_smoothness` 历史模拟；复核 [token-consumption-audit.md](audits/token-consumption-audit.md)。ISSUE-003（本地 fallback 未接 main）仍为待办。完成报告：[W-002-完成报告](../templates/Codex完成报告/W-002-完成报告.md)。

**WebSocket 修复（2026-05-29，W-006 retroactive 验收）**：`app/web_console.py` 改用 Starlette `WebSocketRoute`；`web/static/app.js` 1008→`refreshSession()`。W-006 新增 pytest WS 回归替代 ephemeral `ws_probe`。完成报告：[Web控制台WebSocket修复-完成报告](../templates/Codex完成报告/Web控制台WebSocket修复-完成报告.md)。

W-004（仍有效）：MiMo 请求对齐、`mimo-v2.5` 默认；使用 MiMo 须选预设、填 Key 并**保存**。

## 下一个推荐工单

**可选**：补充 `mimo-v2-flash` 或移除将下线的 `mimo-v2-omni`（ISSUE-004 剩余）。**Web 文案**：ISSUE-002 节奏 Tab 歧义。**W-LINT-001**：一次性 `ruff check --fix` 解决 ISSUE-041 9 处 `I001`（8 个未碰的历史文件）。**W-NICKNAME-EXT**：昵称在 OBS 直播伴侣 SSE 弹幕标签中区分发言者（独立工单，需改协议）。

## 需要复核的文档

| 文档 | 原因 |
|------|------|
| [docs/archive/](archive/) | 含已移除 Qt 主窗、实时弹幕模式等历史设计，**非当前行为** |
| [archive/planning/](archive/planning/) | 功能计划可能已 superseded |

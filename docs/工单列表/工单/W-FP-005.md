# W-FP-005 — 悬浮窗实机空白与 status 指标修复

> **来源**：W-FP-004 实机验收未闭环（窗体可见但空白、`display_count` 恒 0）  
> **状态**：已完成  
> **优先级**：高

## 目标

- 修复 `floating_panel` 模式下悬浮窗集成态空白（首帧可见 + timer 重试）
- `/api/status` 按 `display_mode` 语义化 `display_count`，并暴露悬浮窗/横向分项指标
- `DANMU_FLOATING_PANEL_DEBUG=1` 集成诊断日志

## 验收

- [x] 992 pytest passed；boundary_guard PASS
- [ ] 实机：`display_mode=floating_panel` 30s 内悬浮窗出字（待负责人复验）
- [ ] `POST /api/test/danmu` 后 `floating_panel_active_count > 0`

## 关联

- W-FP-004 标为「部分完成」；本工单接续实机闭环

# W-FP-V2-001 — 悬浮窗模式 V2（bililive_dm 式 Engine/Overlay）

> **状态**：执行中  
> **策略**：替换（分阶段）；`app/floating_panel.py` 保留 deprecated 壳，不硬删

## 目标

以 `danmu_render_mode`（scrolling / floating_panel）互斥模式替换 W-FP 单体悬浮窗；新增 `FloatingPanelEngine` + `FloatingPanelOverlay`；`_display_danmu_text()` 路由器接入主链路。

## 迁移规则

| display_mode（只读遗留） | danmu_render_mode |
|--------------------------|-------------------|
| overlay | scrolling |
| floating_panel | floating_panel |
| both | scrolling（both 已废弃） |

## 验收

- [ ] 指定 pytest 全绿
- [ ] `python scripts/boundary_guard.py` PASS
- [ ] `docs/reports/floating-panel-mode-report.md` 已交付

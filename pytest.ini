# pytest 全局配置（最简形式；其它 hook 在 conftest.py / tests/conftest.py 中）
# testpaths = tests   — 仅收集 tests/，忽略根目录散落脚本
# pythonpath = .      — 允许 ``import app`` / ``from main import ...``，无须 PYTHONPATH
[pytest]
testpaths = tests
pythonpath = .

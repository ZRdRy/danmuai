# Supabase — 公告与反馈

Apply `migrations/001_announcements_feedback.sql` to your project (or use Supabase MCP `apply_migration`).

Copy `../web/static/supabase-config.example.js` to `../web/static/supabase-config.js` and set `url` + `anonKey`.
